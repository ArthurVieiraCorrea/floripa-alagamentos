---
phase: 08-backend-resilience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/services/forecastService.js
autonomous: true
requirements:
  - RESIL-02
must_haves:
  truths:
    - "Falha na tentativa 1 do Open-Meteo aguarda 5s antes de tentar novamente"
    - "Falha na tentativa 2 aguarda 10s antes da tentativa final"
    - "Falha na tentativa 3 não aguarda — lança o erro (cache stale continua sendo servido)"
    - "O log de cada retry imprime o tempo real de espera, não o valor fixo da constante"
  artifacts:
    - path: "backend/src/services/forecastService.js"
      provides: "fetchWithRetry() com backoff exponencial"
      contains: "Math.pow(2, attempt - 1)"
  key_links:
    - from: "fetchWithRetry()"
      to: "sleep(waitMs)"
      via: "variável waitMs = RETRY_DELAY_MS * Math.pow(2, attempt - 1)"
      pattern: "Math\\.pow\\(2, attempt - 1\\)"
---

<objective>
Converter o retry de delay fixo em backoff exponencial no `fetchWithRetry()` do forecastService.

Purpose: Cumprir RESIL-02 — o sistema deve recuar progressivamente antes de cada tentativa de
reconexão ao Open-Meteo, reduzindo carga sobre a API remota durante falhas transitórias. O
comportamento atual (delay fixo de 5s) já implementa retry, mas sem backoff.

Output: `backend/src/services/forecastService.js` com `sleep(RETRY_DELAY_MS * Math.pow(2, attempt - 1))`
e log mostrando o tempo real de espera calculado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-backend-resilience/08-CONTEXT.md
@.planning/phases/08-backend-resilience/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implementar backoff exponencial em fetchWithRetry()</name>
  <files>backend/src/services/forecastService.js</files>
  <action>
Editar o bloco `if (attempt < MAX_RETRIES)` dentro do catch de `fetchWithRetry()` (linhas 54-57).

Estado atual (linhas 54-57):
```javascript
if (attempt < MAX_RETRIES) {
  console.warn(`[forecast] Tentativa ${attempt}/${MAX_RETRIES} falhou: ${err.message}. Aguardando ${RETRY_DELAY_MS / 1000}s...`);
  await sleep(RETRY_DELAY_MS);
}
```

Substituir pelo seguinte (mantendo o guard `if (attempt < MAX_RETRIES)` e a indentação existente):
```javascript
if (attempt < MAX_RETRIES) {
  const waitMs = RETRY_DELAY_MS * Math.pow(2, attempt - 1);
  console.warn(`[forecast] Tentativa ${attempt}/${MAX_RETRIES} falhou: ${err.message}. Aguardando ${waitMs / 1000}s...`);
  await sleep(waitMs);
}
```

Mudanças exatas:
1. Adicionar `const waitMs = RETRY_DELAY_MS * Math.pow(2, attempt - 1);` como primeira linha dentro do if
2. Substituir `${RETRY_DELAY_MS / 1000}s` por `${waitMs / 1000}s` no console.warn
3. Substituir `await sleep(RETRY_DELAY_MS)` por `await sleep(waitMs)`

NADA mais muda no arquivo. Não alterar constantes, não alterar fetchAndCacheForecasts(), não
alterar o bloco do throw final.

Comportamento resultante (per D-01):
- attempt=1 falha → waitMs = 5000 * 2^0 = 5000ms → dorme 5s
- attempt=2 falha → waitMs = 5000 * 2^1 = 10000ms → dorme 10s
- attempt=3 falha → guard false (3 não é menor que MAX_RETRIES=3) → sem sleep → throw lastErr

O cache stale continua sendo servido via fetchAndCacheForecasts() que captura o throw — nenhuma
mudança necessária nessa camada (D-02, D-03 já implementados, não tocar).
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const src = fs.readFileSync('backend/src/services/forecastService.js', 'utf8'); if (!src.includes('Math.pow(2, attempt - 1)')) throw new Error('MISSING: Math.pow(2, attempt - 1)'); if (src.includes('await sleep(RETRY_DELAY_MS)')) throw new Error('OLD CODE STILL PRESENT: await sleep(RETRY_DELAY_MS)'); if (src.includes('RETRY_DELAY_MS / 1000}s')) throw new Error('OLD LOG STILL PRESENT'); console.log('OK — backoff exponencial aplicado');"</automated>
  </verify>
  <done>
    - `fetchWithRetry()` usa `RETRY_DELAY_MS * Math.pow(2, attempt - 1)` para calcular o delay
    - O log de cada retry imprime o `waitMs` real (5s na tentativa 1, 10s na tentativa 2)
    - O arquivo ainda carrega sem erro de sintaxe (`node -e "require('./backend/src/services/forecastService.js'); console.log('OK')"` retorna OK)
    - Nenhuma outra função do arquivo foi alterada
  </done>
</task>

</tasks>

<threat_model>
## Threat Model

| Threat | STRIDE | Mitigation |
|--------|--------|------------|
| Open-Meteo indisponível por período prolongado — retries exaurem sem impacto ao usuário | Denial of Service | Cache stale continua sendo servido por fetchAndCacheForecasts() que nunca faz throw (D-02). Backoff exponencial reduz pressão sobre o endpoint remoto durante falhas. |
| Log de retry revela timing interno e padrão de falhas | Information Disclosure | Logs ficam em servidor; não são expostos ao frontend. Aceitável para operação. |
</threat_model>

<verification>
Após aplicar o patch, executar:

```bash
# 1. Verificar sintaxe e carregamento do módulo
node -e "require('./backend/src/services/forecastService.js'); console.log('Módulo carrega OK')"

# 2. Confirmar que o backoff exponencial está presente no código
node -e "const fs = require('fs'); const src = fs.readFileSync('backend/src/services/forecastService.js', 'utf8'); if (!src.includes('Math.pow(2, attempt - 1)')) throw new Error('MISSING'); console.log('Backoff exponencial: OK');"

# 3. Confirmar que o log fixo antigo foi removido
node -e "const fs = require('fs'); const src = fs.readFileSync('backend/src/services/forecastService.js', 'utf8'); if (src.includes('await sleep(RETRY_DELAY_MS)')) throw new Error('OLD CODE PRESENT'); console.log('Código antigo removido: OK');"
```

Sequência de delays esperada com MAX_RETRIES=3 e RETRY_DELAY_MS=5000:
- Tentativa 1 falha → 5000 * 2^0 = 5000ms (5s)
- Tentativa 2 falha → 5000 * 2^1 = 10000ms (10s)
- Tentativa 3 falha → sem sleep (guard `attempt < MAX_RETRIES` é false) → throw
</verification>

<success_criteria>
- `fetchWithRetry()` implementa backoff exponencial (5s → 10s → falha) per D-01
- O log de cada retry exibe o tempo real de espera calculado (não o valor da constante)
- `fetchAndCacheForecasts()` continua nunca fazendo throw — cache stale permanece válido per D-02
- `node -e "require('./backend/src/services/forecastService.js')"` não lança erro
- Nenhum outro comportamento do serviço foi alterado
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/08-backend-resilience/08-01-SUMMARY.md` com:
- Mudança exata aplicada (diff das linhas alteradas)
- Confirmação dos três testes de verificação passando
- Observação sobre o comportamento de attempt=3 (sem sleep, guard falso)
</output>
