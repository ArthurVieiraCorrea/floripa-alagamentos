---
phase: 09-frontend-ux
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - backend/src/routes/alertas.js
  - frontend/src/services/api.js
  - frontend/index.html
  - frontend/src/main.js
  - frontend/src/styles/main.css
autonomous: true
requirements:
  - UX-03

must_haves:
  truths:
    - "GET /api/alertas/historico retorna alertas do usuário autenticado paginados, 20/página, enviado_em DESC"
    - "Endpoint retorna apenas alertas do usuário da sessão (não de outros usuários)"
    - "Aba 'Alertas' aparece na sidebar para usuário autenticado"
    - "Lista exibe cards com bairro, enviado_em formatado em pt-BR e summary do evento"
    - "Estado vazio exibe 'Você ainda não recebeu alertas.' sem quebrar o layout"
    - "Paginação usa #paginacao-alertas e state.paginaAlertas — sem conflito com #paginacao de ocorrências"
    - "Aba invisível para usuário não autenticado"
  artifacts:
    - path: "backend/src/routes/alertas.js"
      provides: "GET /historico route com requireAuth, paginação, WHERE usuario_id"
      contains: "router.get.*historico"
    - path: "frontend/src/services/api.js"
      provides: "api.alertas.historico(pagina)"
      contains: "historico"
    - path: "frontend/index.html"
      provides: "Botão #tab-btn-alertas e div #tab-alertas com #lista-alertas e #paginacao-alertas"
      contains: "tab-btn-alertas"
    - path: "frontend/src/main.js"
      provides: "carregarHistoricoAlertas(), renderizarListaAlertas(), state.paginaAlertas, wiring em carregarSessao() e tab handler"
      contains: "carregarHistoricoAlertas"
    - path: "frontend/src/styles/main.css"
      provides: "CSS para .lista-alertas, .card-alerta, .card-top, .card-bairro, .card-data, .card-desc"
      contains: ".card-alerta"
  key_links:
    - from: "frontend/src/main.js carregarHistoricoAlertas()"
      to: "GET /api/alertas/historico"
      via: "api.alertas.historico(state.paginaAlertas)"
      pattern: "api.alertas.historico"
    - from: "backend/src/routes/alertas.js /historico"
      to: "alertas_enviados"
      via: "db.all WHERE usuario_id = req.session.userId"
      pattern: "usuario_id.*session"
    - from: "#paginacao-alertas buttons"
      to: "carregarHistoricoAlertas()"
      via: "state.paginaAlertas = parseInt(btn.dataset.p)"
      pattern: "paginaAlertas"
---

<objective>
Implementar a aba "Alertas" (UX-03): endpoint backend GET /api/alertas/historico + aba frontend com lista paginada dos alertas recebidos pelo usuário, exibindo bairro, horário e evento do calendar.

Purpose: Dar ao usuário visibilidade sobre quais alertas recebeu, quando e para qual evento, com paginação para históricos longos.
Output: Endpoint backend novo, serviço api.js atualizado, aba Alertas funcional com cards e paginação separada das ocorrências.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-frontend-ux/09-CONTEXT.md
@.planning/phases/09-frontend-ux/09-RESEARCH.md

Decisões locked (não renegociar):
- D-07: Nova aba "Alertas", auth-only
- D-08: GET /api/alertas/historico — todos os alertas do usuário (incluindo vistos), 20/página, enviado_em DESC, ?pagina=N, requireAuth
- D-09: Campos: bairro, enviado_em (pt-BR), summary. SEM score de risco.
- D-10: Empty state: "Você ainda não recebeu alertas."
- D-11: Paginação segue padrão existente MAS usa #paginacao-alertas + state.paginaAlertas (não #paginacao)
</context>

<interfaces>
<!-- Extraído de backend/src/routes/alertas.js para referência imediata -->

```javascript
// Padrão existente — /pendentes como modelo:
router.get('/pendentes', requireAuth, (req, res) => {
  const db = getDb();
  try {
    const alertas = db.all(
      `SELECT id, google_event_id, bairro, score, summary, enviado_em
         FROM alertas_enviados
        WHERE usuario_id = ?
          AND visto_em IS NULL
        ORDER BY enviado_em DESC
        LIMIT 20`,
      [req.session.userId]
    );
    res.json({ alertas: alertas || [] });
  } catch (err) {
    console.error('[alertas] Erro ao buscar pendentes:', err.message);
    res.status(500).json({ erro: 'Erro interno' });
  }
});
// module.exports = router; — linha 71, inserir /historico ANTES dessa linha
```

<!-- alertas_enviados schema (verificado em database.js lines 150-167): -->
```
id INTEGER PK | usuario_id INTEGER FK | bairro TEXT | summary TEXT (nullable)
enviado_em TEXT (SQLite datetime, UTC sem Z) | visto_em TEXT (nullable)
score REAL | google_event_id TEXT | risk_cycle_key TEXT
```
<!-- Índice já existente: idx_alertas_enviados_usuario_em ON alertas_enviados(usuario_id, enviado_em) -->
<!-- /historico NÃO filtra por visto_em — retorna TODOS (D-08) -->

<!-- Extraído de frontend/src/services/api.js (lines 73-77): -->
```javascript
alertas: {
  pendentes: () => request('/alertas/pendentes'),
  marcarVisto: (ids) =>
    request('/alertas/marcar-visto', { method: 'POST', body: JSON.stringify({ ids }) }),
  // ADICIONAR: historico() aqui
},
```

<!-- Padrão de paginação de frontend/src/main.js (lines 183-251): -->
```javascript
// renderizarLista usa state.paginaAtual e #paginacao
// renderizarListaAlertas DEVE usar state.paginaAlertas e #paginacao-alertas
// para evitar colisão (pitfall 1 da RESEARCH.md)
```

<!-- formatarData já existe em main.js (line 178): -->
```javascript
function formatarData(str) {
  const d = new Date(str.replace(' ', 'T'));
  return d.toLocaleString('pt-BR');
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Backend — Adicionar GET /api/alertas/historico em alertas.js</name>
  <files>backend/src/routes/alertas.js</files>
  <action>
Em `backend/src/routes/alertas.js`, inserir a rota GET /historico ANTES da linha `module.exports = router;` (linha 71):

```javascript
// GET /api/alertas/historico
// Retorna todos os alertas do usuário autenticado (incluindo vistos), 20 por página.
// Parâmetro: ?pagina=N (padrão: 1)
// Resposta: { alertas: [...], paginacao: { pagina, paginas, total } }
router.get('/historico', requireAuth, (req, res) => {
  const db = getDb();
  const pagina = Math.max(1, parseInt(req.query.pagina) || 1);
  const limite = 20;
  const offset = (pagina - 1) * limite;
  try {
    const total = db.get(
      `SELECT COUNT(*) as total FROM alertas_enviados WHERE usuario_id = ?`,
      [req.session.userId]
    );
    const alertas = db.all(
      `SELECT id, bairro, summary, enviado_em
         FROM alertas_enviados
        WHERE usuario_id = ?
        ORDER BY enviado_em DESC
        LIMIT ? OFFSET ?`,
      [req.session.userId, limite, offset]
    );
    const totalPaginas = Math.ceil((total?.total || 0) / limite);
    res.json({
      alertas: alertas || [],
      paginacao: { pagina, paginas: totalPaginas, total: total?.total || 0 }
    });
  } catch (err) {
    console.error('[alertas] Erro ao buscar historico:', err.message);
    res.status(500).json({ erro: 'Erro interno' });
  }
});
```

Checklist crítico:
- `WHERE usuario_id = ?` com `req.session.userId` — nunca omitir (pitfall 6: vazamento de dados entre usuários)
- SEM filtro `visto_em IS NULL` — retornar TODOS os alertas (D-08 especifica "incluindo os já vistos")
- SELECT inclui apenas: id, bairro, summary, enviado_em — sem `score` (D-09)
- `Math.max(1, ...)` para pagina — defesa contra paginação negativa
  </action>
  <verify>
    - `grep -n "router.get.*historico" backend/src/routes/alertas.js` retorna 1 linha
    - `grep -n "usuario_id" backend/src/routes/alertas.js` — deve aparecer em /historico
    - `grep -n "visto_em" backend/src/routes/alertas.js` — NÃO deve aparecer na rota /historico (apenas em /pendentes e /marcar-visto)
    - Com servidor rodando: `curl -s http://localhost:3000/api/alertas/historico` retorna 401 (não autenticado)
    - Autenticado (via browser ou cookie de sessão): GET /api/alertas/historico?pagina=1 retorna JSON com campos `alertas` e `paginacao`
  </verify>
  <done>GET /api/alertas/historico retorna 401 sem auth; retorna { alertas: [], paginacao: { pagina: 1, paginas: 0, total: 0 } } para usuário sem histórico; retorna dados paginados corretos para usuário com alertas.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend — api.js + HTML + main.js + CSS para aba Alertas</name>
  <files>frontend/src/services/api.js, frontend/index.html, frontend/src/main.js, frontend/src/styles/main.css</files>
  <action>
**api.js — Adicionar api.alertas.historico():**
Em `frontend/src/services/api.js`, no objeto `alertas` (linhas 73-77), adicionar o método `historico` após `marcarVisto`:
```javascript
historico: (pagina = 1) => request(`/alertas/historico?pagina=${pagina}`),
```
O objeto `alertas` final deve ter três métodos: `pendentes`, `marcarVisto`, `historico`.

---

**index.html — Botão de aba e painel:**
1. Na div `.tabs`, inserir botão Alertas APÓS o botão de Status (adicionado no plano 09-01) e ANTES do botão Admin. Ordem final: Registrar, Histórico, Calendário, Status, **Alertas**, Admin.
```html
<button class="tab" data-tab="alertas" id="tab-btn-alertas" style="display:none">Alertas</button>
```

2. Após o fechamento do painel `#tab-status` (adicionado no plano 09-01), inserir o painel da aba Alertas:
```html
<!-- UX-03: Histórico de alertas recebidos — auth-only -->
<div class="tab-content" id="tab-alertas">
  <h2>Histórico de Alertas</h2>
  <div id="lista-alertas" class="lista-alertas">
    <p class="loading">Carregando...</p>
  </div>
  <div id="paginacao-alertas" class="paginacao"></div>
</div>
```
Confirmar: `data-tab="alertas"` no botão → `id="tab-alertas"` no painel (template literal do tab listener).

---

**main.js — Adicionar state.paginaAlertas, funções e wiring:**

**Adição 1 — state object:** Localizar o objeto `state` na inicialização (deve conter `paginaAtual` e outros campos). Adicionar:
```javascript
paginaAlertas: 1,
```

**Adição 2 — Funções carregarHistoricoAlertas() e renderizarListaAlertas():** Inserir próximo às outras funções de carregamento (por exemplo, após a função `carregarStatus` adicionada no plano 09-01):
```javascript
// ── Histórico de alertas (UX-03) ─────────────────────────
async function carregarHistoricoAlertas() {
  const lista = document.getElementById('lista-alertas');
  const pag   = document.getElementById('paginacao-alertas');
  lista.innerHTML = '<p class="loading">Carregando...</p>';
  try {
    const resp = await api.alertas.historico(state.paginaAlertas);
    renderizarListaAlertas(resp.alertas, resp.paginacao);
  } catch (e) {
    lista.innerHTML = `<p class="loading" style="color:#fca5a5">Erro: ${e.message}</p>`;
    pag.innerHTML = '';
  }
}

function renderizarListaAlertas(alertas, paginacao) {
  const lista = document.getElementById('lista-alertas');
  const pag   = document.getElementById('paginacao-alertas');

  if (!alertas.length) {
    lista.innerHTML = '<p class="loading">Você ainda não recebeu alertas.</p>';
    pag.innerHTML = '';
    return;
  }

  lista.innerHTML = alertas.map(a => `
    <div class="card-alerta">
      <div class="card-top">
        <span class="card-bairro">${String(a.bairro || '').replace(/</g, '&lt;').replace(/>/g, '&gt;')}</span>
        <span class="card-data">${formatarData(a.enviado_em)}</span>
      </div>
      ${a.summary ? `<div class="card-desc">${String(a.summary).replace(/</g, '&lt;').replace(/>/g, '&gt;')}</div>` : ''}
    </div>
  `).join('');

  const totalPags = paginacao.paginas;
  if (totalPags <= 1) { pag.innerHTML = ''; return; }

  pag.innerHTML = Array.from({ length: totalPags }, (_, i) => i + 1)
    .map(p => `<button class="${p === state.paginaAlertas ? 'active' : ''}" data-p="${p}">${p}</button>`)
    .join('');

  pag.querySelectorAll('button').forEach(btn => {
    btn.addEventListener('click', () => {
      state.paginaAlertas = parseInt(btn.dataset.p);
      carregarHistoricoAlertas();
    });
  });
}
```
Nota de segurança: bairro e summary são escapados com `replace(/</g, '&lt;')` para prevenir XSS.

**Adição 3 — carregarSessao() — exibir/ocultar aba Alertas:**
Localizar as linhas onde tab-btn-status é mostrado/ocultado (adicionadas no plano 09-01). Adicionar imediatamente após cada uma:
```javascript
// No bloco if (usuario), após tab-btn-status:
document.getElementById('tab-btn-alertas').style.display = 'inline-block';

// No bloco else, após tab-btn-status:
document.getElementById('tab-btn-alertas').style.display = 'none';
```

**Adição 4 — Tab click handler:**
No bloco `document.querySelectorAll('.tab').forEach(...)`, após a linha `if (tab.dataset.tab === 'status') carregarStatus();` (adicionada no plano 09-01), adicionar:
```javascript
if (tab.dataset.tab === 'alertas') {
  state.paginaAlertas = 1;
  carregarHistoricoAlertas();
}
```

---

**main.css — Adicionar CSS dos cards de alerta:**
Após o bloco CSS do Status panel (adicionado no plano 09-01), acrescentar ao final de `frontend/src/styles/main.css`:
```css
/* ── Phase 9 UX-03: Alertas tab ──────────────────────────── */
.lista-alertas {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.card-alerta {
  background: var(--surface2);
  border: 1px solid var(--border);
  border-left: 4px solid var(--accent);
  border-radius: 6px;
  padding: 10px 12px;
}

.card-top {
  display: flex;
  justify-content: space-between;
  align-items: baseline;
  gap: .5rem;
  margin-bottom: .25rem;
}

.card-bairro {
  font-weight: 600;
  font-size: .875rem;
  color: var(--text);
}

.card-data {
  font-size: .75rem;
  color: var(--text-muted);
  white-space: nowrap;
}

.card-desc {
  font-size: .8rem;
  color: var(--text-muted);
}
```
  </action>
  <verify>
    - `grep -n "historico" frontend/src/services/api.js` retorna 1 linha com a definição do método
    - `grep -n "tab-btn-alertas" frontend/index.html frontend/src/main.js` — aparece em ambos os arquivos
    - `grep -n "paginacao-alertas\|paginaAlertas" frontend/src/main.js` — pelo menos 4 ocorrências
    - `grep -n "card-alerta" frontend/src/styles/main.css` — regra CSS presente
    - `grep -n "Você ainda não recebeu alertas" frontend/src/main.js` — 1 ocorrência
    - Com servidor rodando e usuário autenticado: clicar aba Alertas exibe lista ou mensagem de vazio sem erro de console
    - Paginação de Alertas não interfere com paginação de Histórico de Ocorrências (testar clicando em páginas em ambas as abas alternadamente)
  </verify>
  <done>
    api.alertas.historico() disponível; botão #tab-btn-alertas exibido para usuários autenticados; aba Alertas carrega dados reais via GET /api/alertas/historico; cards exibem bairro, data em pt-BR e summary; estado vazio exibe mensagem correta; paginação usa #paginacao-alertas e state.paginaAlertas sem colidir com ocorrências.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Aba "Alertas" completa (UX-03): endpoint backend GET /api/alertas/historico, api.alertas.historico(), aba frontend com lista paginada de cards, paginação independente, estado vazio, CSS.
  </what-built>
  <how-to-verify>
    1. Abrir http://localhost:3000 no browser
    2. Verificar que a aba "Alertas" NÃO aparece sem login
    3. Fazer login com Google
    4. Verificar que o botão "Alertas" aparece na sidebar, entre "Status" e "Admin"
    5. Clicar na aba "Alertas"
    6. Se o usuário não tem alertas: confirmar que aparece "Você ainda não recebeu alertas." (D-10)
    7. Se o usuário tem alertas: confirmar que cada card mostra bairro (destaque), data em português e nome do evento (summary)
    8. Se há mais de 20 alertas: confirmar que botões de paginação aparecem em #paginacao-alertas
    9. Abrir a aba "Histórico" (de ocorrências) e navegar entre páginas — confirmar que isso NÃO afeta a paginação da aba Alertas
    10. Abrir a aba "Alertas" novamente e navegar entre páginas — confirmar que isso NÃO afeta a aba Histórico
    11. Fazer logout e confirmar que a aba "Alertas" desaparece
    12. Abrir DevTools → Network → confirmar que GET /api/alertas/historico retorna 200 com JSON contendo `alertas` e `paginacao`
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → GET /api/alertas/historico | Usuário autenticado consulta seu próprio histórico; parâmetro ?pagina controlado pelo usuário |
| Backend → alertas_enviados | Query SQL com WHERE usuario_id = req.session.userId |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Elevation of Privilege | GET /historico sem auth | mitigate | requireAuth middleware já em uso — retorna 401 sem sessão válida |
| T-09-02-02 | Information Disclosure | WHERE usuario_id vazamento | mitigate | Query inclui `WHERE usuario_id = ?` com `req.session.userId`; nunca usar ID da URL ou query string para isolar dados |
| T-09-02-03 | Injection | ?pagina=N parâmetro | mitigate | `parseInt(...) || 1` + `Math.max(1, ...)` — nunca interpolado em SQL; passado como bind parameter `?` |
| T-09-02-04 | XSS | bairro/summary renderizados em innerHTML | mitigate | Ambos os campos escapados com `.replace(/</g, '&lt;').replace(/>/g, '&gt;')` antes de inserção no DOM |
| T-09-02-05 | Information Disclosure | score excluído do SELECT | accept | D-09 deliberadamente exclui score; SELECT lista apenas id, bairro, summary, enviado_em |
</threat_model>

<verification>
Após execução:
1. `grep "router.get.*historico" backend/src/routes/alertas.js` — 1 resultado
2. `grep "usuario_id.*session\|session.*userId" backend/src/routes/alertas.js` — aparece na rota /historico
3. `grep "historico" frontend/src/services/api.js` — método presente
4. `grep "paginacao-alertas" frontend/index.html frontend/src/main.js` — aparece em ambos
5. `grep "card-alerta" frontend/src/styles/main.css` — CSS presente
6. Teste funcional: aba Alertas carrega dados sem erro de console; paginação de alertas e de ocorrências não interferem
</verification>

<success_criteria>
- GET /api/alertas/historico retorna 401 sem auth, 200 com dados corretos para usuário autenticado (per D-08)
- Endpoint retorna apenas alertas do usuario da sessão — nunca de outros usuários (per D-08, pitfall 6 evitado)
- Aba "Alertas" aparece para usuário autenticado, entre Status e Admin (per D-07)
- Cards exibem bairro, enviado_em em pt-BR e summary — sem score (per D-09)
- Estado vazio exibe "Você ainda não recebeu alertas." (per D-10)
- Paginação usa #paginacao-alertas + state.paginaAlertas — sem colisão com ocorrências (per D-11, pitfall 1 evitado)
- Aba invisível para usuário não autenticado (per D-07)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/09-frontend-ux/09-02-SUMMARY.md` seguindo o template em `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
