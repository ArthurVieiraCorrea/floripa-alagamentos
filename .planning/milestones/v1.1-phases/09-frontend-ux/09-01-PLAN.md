---
phase: 09-frontend-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/index.html
  - frontend/src/main.js
  - frontend/src/styles/main.css
autonomous: true
requirements:
  - UX-02

must_haves:
  truths:
    - "Usuário autenticado vê a aba 'Status' na sidebar, entre Calendário e Admin"
    - "Aba Status mostra três indicadores: Previsão meteorológica, Google Calendar, Notificações push"
    - "Indicador de Previsão fica vermelho quando stale e verde quando atualizado"
    - "Indicador de Calendário reflete calendar_connected/calendar_disconnected de state.usuario"
    - "Indicador de Push consulta swRegistration.pushManager.getSubscription() em tempo real"
    - "Aba Status não aparece para usuário não autenticado"
    - "Painel é somente leitura — nenhum botão de ação inline"
  artifacts:
    - path: "frontend/index.html"
      provides: "Botão tab #tab-btn-status e div #tab-status com três .status-indicador"
      contains: "tab-btn-status"
    - path: "frontend/src/main.js"
      provides: "carregarStatus() e wiring em carregarSessao() e tab click handler"
      contains: "carregarStatus"
    - path: "frontend/src/styles/main.css"
      provides: "CSS para .status-indicadores, .status-indicador, .status-dot, .status-info, .status-label, .status-detalhe"
      contains: ".status-indicador"
  key_links:
    - from: "frontend/src/main.js carregarSessao()"
      to: "#tab-btn-status"
      via: "style.display = 'inline-block' / 'none'"
      pattern: "tab-btn-status.*display"
    - from: "tab click handler"
      to: "carregarStatus()"
      via: "if (tab.dataset.tab === 'status')"
      pattern: "dataset.tab.*status"
---

<objective>
Implementar a aba "Status" (UX-02): nova aba autenticada na sidebar mostrando três indicadores de leitura sobre o estado do sistema — frescor da previsão meteorológica, conexão ao Google Calendar e push ativo/inativo.

Purpose: Dar ao usuário visibilidade sobre se o sistema está operando corretamente, sem poluir a aba Calendário.
Output: Aba Status funcional com indicadores coloridos, visível apenas para usuário autenticado, sem botões de ação.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-frontend-ux/09-CONTEXT.md
@.planning/phases/09-frontend-ux/09-RESEARCH.md

Decisões locked (não renegociar):
- D-03: Nova aba "Status", auth-only, mesmo padrão que aba Admin
- D-04: Três indicadores: Forecast (api.previsao.atual()), Calendário (state.usuario), Push (swRegistration)
- D-05: Sem botões de ação — somente leitura
- D-06: Botão da aba Status posicionado após Calendário, antes de Admin
</context>

<interfaces>
<!-- Extraído de frontend/src/main.js para uso imediato pelo executor — sem exploração adicional necessária -->

<!-- Tab system (lines 319-328): -->
```javascript
document.querySelectorAll('.tab').forEach(tab => {
  tab.addEventListener('click', () => {
    document.querySelectorAll('.tab').forEach(t => t.classList.remove('active'));
    document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('active'));
    tab.classList.add('active');
    document.getElementById(`tab-${tab.dataset.tab}`).classList.add('active');
    if (tab.dataset.tab === 'historico') carregarHistorico();
    if (tab.dataset.tab === 'calendario') verificarAlertasPendentes();
  });
});
```

<!-- Auth-gated tab visibility (lines 514-527): -->
```javascript
// On login (inside if (usuario) block, after tab-btn-admin line 519):
document.getElementById('tab-btn-admin').style.display = 'inline-block';
// On logout (inside else block, after tab-btn-admin line 525):
document.getElementById('tab-btn-admin').style.display = 'none';
```

<!-- Tab buttons in index.html (lines 71-76): -->
```html
<div class="tabs">
  <button class="tab active" data-tab="registrar">Registrar</button>
  <button class="tab" data-tab="historico">Histórico</button>
  <button class="tab" data-tab="calendario">Calendário</button>
  <button class="tab" data-tab="admin" id="tab-btn-admin" style="display:none">Admin</button>
</div>
```

<!-- CSS variables from main.css (lines 1-23): -->
```css
--bg: #0f172a; --surface: #1e293b; --surface2: #263549;
--border: #334155; --text: #e2e8f0; --text-muted: #94a3b8; --accent: #3b82f6;
--risco-verde: #22c55e; --risco-amarelo: #f59e0b; --risco-vermelho: #ef4444;
```

<!-- state.usuario fields (from /api/auth/me): -->
```javascript
// state.usuario = { id, email, nome, alert_threshold, alert_hours_before,
//                   calendar_connected (0|1), calendar_disconnected (0|1) }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: HTML — Adicionar botão e painel da aba Status em index.html</name>
  <files>frontend/index.html</files>
  <action>
Em `frontend/index.html`, fazer dois acréscimos:

1. Na div `.tabs` (linha 71-76), inserir o botão da aba Status ENTRE o botão `data-tab="calendario"` e o botão `data-tab="admin"`:
```html
<button class="tab" data-tab="status" id="tab-btn-status" style="display:none">Status</button>
```
A ordem final dos botões deve ser: Registrar, Histórico, Calendário, **Status**, Admin.

2. Após o fechamento da div `#tab-calendario` (buscar `id="tab-calendario"` para localizar onde termina esse bloco), inserir o painel da aba Status como nova div `.tab-content`:
```html
<!-- UX-02: Status do sistema — apenas leitura, auth-only -->
<div class="tab-content" id="tab-status">
  <h2>Status do Sistema</h2>
  <div id="status-indicadores" class="status-indicadores">
    <div class="status-indicador">
      <span class="status-dot" id="status-dot-forecast">&#9679;</span>
      <div class="status-info">
        <span class="status-label">Previsão meteorológica</span>
        <span class="status-detalhe" id="status-detalhe-forecast">Verificando...</span>
      </div>
    </div>
    <div class="status-indicador">
      <span class="status-dot" id="status-dot-calendar">&#9679;</span>
      <div class="status-info">
        <span class="status-label">Google Calendar</span>
        <span class="status-detalhe" id="status-detalhe-calendar">—</span>
      </div>
    </div>
    <div class="status-indicador">
      <span class="status-dot" id="status-dot-push">&#9679;</span>
      <div class="status-info">
        <span class="status-label">Notificações push</span>
        <span class="status-detalhe" id="status-detalhe-push">—</span>
      </div>
    </div>
  </div>
</div>
```

Confirmar que `data-tab="status"` no botão corresponde a `id="tab-status"` no painel (o tab listener usa template literal `tab-${tab.dataset.tab}`).
  </action>
  <verify>
    Abrir index.html e confirmar:
    - `grep -n "tab-btn-status" frontend/index.html` retorna uma linha com `style="display:none"`
    - `grep -n "tab-status" frontend/index.html` retorna pelo menos duas linhas (botão + div)
    - Ordem dos botões na div `.tabs`: registrar, historico, calendario, **status**, admin
    - `grep -n "status-dot-forecast\|status-dot-calendar\|status-dot-push" frontend/index.html` retorna 3 linhas
  </verify>
  <done>index.html contém botão #tab-btn-status (hidden) e painel #tab-status com 3 indicadores; botão está após Calendário e antes de Admin.</done>
</task>

<task type="auto">
  <name>Task 2: JS — Implementar carregarStatus() e wiring de sessão/tabs em main.js</name>
  <files>frontend/src/main.js</files>
  <action>
Fazer três adições em `frontend/src/main.js`:

**Adição 1 — Função carregarStatus() — inserir próximo às outras funções de carregamento de abas (por exemplo, após carregarCalendario ou antes do bloco "Init"):**
```javascript
// ── Status do sistema (UX-02) ────────────────────────────
async function carregarStatus() {
  // Forecast freshness — chama API (já cacheada pelo browser, custo baixo)
  const dotF = document.getElementById('status-dot-forecast');
  const detF = document.getElementById('status-detalhe-forecast');
  try {
    const data = await api.previsao.atual();
    if (data?.stale) {
      dotF.style.color = '#ef4444';  // --risco-vermelho
      const mins = data.last_updated
        ? Math.round((Date.now() - new Date(data.last_updated + 'Z')) / 60000)
        : '?';
      const horas = Math.floor(mins / 60);
      const resto = mins % 60;
      detF.textContent = `Desatualizado (há ${horas > 0 ? horas + 'h' : ''}${resto}min)`;
    } else {
      dotF.style.color = '#22c55e';  // --risco-verde
      detF.textContent = 'Atualizado';
    }
  } catch {
    dotF.style.color = '#94a3b8';  // --text-muted
    detF.textContent = 'Indisponível';
  }

  // Calendar connection — derivado de state.usuario (já populado por carregarSessao)
  const dotC = document.getElementById('status-dot-calendar');
  const detC = document.getElementById('status-detalhe-calendar');
  const u = state.usuario;
  if (u?.calendar_connected === 1 && u?.calendar_disconnected !== 1) {
    dotC.style.color = '#22c55e';
    detC.textContent = 'Conectado';
  } else if (u?.calendar_disconnected === 1) {
    dotC.style.color = '#ef4444';
    detC.textContent = 'Desconectado (token expirado)';
  } else {
    dotC.style.color = '#f59e0b';  // --risco-amarelo
    detC.textContent = 'Não conectado';
  }

  // Push status — consulta subscription em tempo real
  const dotP = document.getElementById('status-dot-push');
  const detP = document.getElementById('status-detalhe-push');
  if (!('Notification' in window) || !('serviceWorker' in navigator)) {
    dotP.style.color = '#94a3b8';
    detP.textContent = 'Não suportado pelo browser';
  } else if (Notification.permission === 'denied') {
    dotP.style.color = '#ef4444';
    detP.textContent = 'Bloqueado pelo browser';
  } else if (swRegistration) {
    const sub = await swRegistration.pushManager.getSubscription();
    dotP.style.color = sub ? '#22c55e' : '#f59e0b';
    detP.textContent = sub ? 'Ativo' : 'Inativo';
  } else {
    dotP.style.color = '#f59e0b';
    detP.textContent = 'Inativo';
  }
}
```

**Adição 2 — carregarSessao() — exibir/ocultar aba Status e Alertas junto com Admin:**
Em `carregarSessao()`, localizar a linha `document.getElementById('tab-btn-admin').style.display = 'inline-block';` (linha ~519) e adicionar imediatamente após:
```javascript
document.getElementById('tab-btn-status').style.display = 'inline-block';
```
Localizar a linha `document.getElementById('tab-btn-admin').style.display = 'none';` (linha ~525) e adicionar imediatamente após:
```javascript
document.getElementById('tab-btn-status').style.display = 'none';
```

NOTA: As linhas para tab-btn-alertas serão adicionadas no Plano 09-02. Não adicionar aqui para manter separação de responsabilidade entre os planos.

**Adição 3 — Tab click handler — adicionar lazy-load para aba status:**
No bloco `document.querySelectorAll('.tab').forEach(tab => {...})` (linha ~319), dentro do callback do addEventListener, após as linhas existentes `if (tab.dataset.tab === 'historico')` e `if (tab.dataset.tab === 'calendario')`, adicionar:
```javascript
if (tab.dataset.tab === 'status') carregarStatus();
```
  </action>
  <verify>
    - `grep -n "carregarStatus" frontend/src/main.js` retorna pelo menos 3 linhas (definição da função + chamada no tab handler + função em si)
    - `grep -n "tab-btn-status" frontend/src/main.js` retorna 2 linhas (show e hide em carregarSessao)
    - `grep -n "dataset.tab.*status\|status.*dataset.tab" frontend/src/main.js` retorna 1 linha
    - Abrir app no browser, fazer login, clicar na aba "Status" e verificar que os três indicadores aparecem com cor (não ficam cinza "Verificando...")
  </verify>
  <done>carregarStatus() definida em main.js; tab-btn-status mostrado/ocultado em carregarSessao(); clicar aba Status dispara carregarStatus() e exibe indicadores coloridos.</done>
</task>

<task type="auto">
  <name>Task 3: CSS — Adicionar estilos para o painel Status em main.css</name>
  <files>frontend/src/styles/main.css</files>
  <action>
Adicionar ao final de `frontend/src/styles/main.css` (após a última regra existente, na linha 482+) o seguinte bloco de CSS:

```css
/* ── Phase 9 UX-02: Status panel ─────────────────────────── */
.status-indicadores {
  display: flex;
  flex-direction: column;
  gap: .75rem;
  margin-top: .5rem;
}

.status-indicador {
  display: flex;
  align-items: flex-start;
  gap: .65rem;
  background: var(--surface2);
  border: 1px solid var(--border);
  border-radius: 6px;
  padding: .65rem .8rem;
}

.status-dot {
  font-size: 1rem;
  line-height: 1.4;
  color: var(--text-muted); /* cor padrão sobrescrita por JS */
  flex-shrink: 0;
}

.status-info {
  display: flex;
  flex-direction: column;
  gap: .1rem;
}

.status-label {
  font-size: .82rem;
  font-weight: 600;
  color: var(--text);
}

.status-detalhe {
  font-size: .75rem;
  color: var(--text-muted);
}
```

Não alterar nenhuma regra CSS existente — apenas acrescentar ao final do arquivo.
  </action>
  <verify>
    - `grep -n "status-indicador\|status-dot\|status-label\|status-detalhe" frontend/src/styles/main.css` retorna pelo menos 5 linhas
    - No browser, aba Status mostra três cards com layout flex lado a lado (● + texto), não inline/crowded
    - Indicadores têm padding visível e fundo levemente diferente do fundo principal (--surface2)
  </verify>
  <done>main.css contém classes .status-indicadores, .status-indicador, .status-dot, .status-info, .status-label e .status-detalhe; indicadores renderizam com layout card organizado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Aba "Status" completa (UX-02): botão de aba inserido entre Calendário e Admin, painel com três indicadores, carregarStatus() que busca dados reais, CSS adequado.
  </what-built>
  <how-to-verify>
    1. Abrir http://localhost:3000 (ou a porta configurada) no browser
    2. Verificar que a aba "Status" NÃO aparece sem login
    3. Fazer login com Google
    4. Verificar que o botão "Status" aparece na sidebar, entre "Calendário" e "Admin"
    5. Clicar na aba "Status"
    6. Verificar que os três indicadores são exibidos: "Previsão meteorológica", "Google Calendar", "Notificações push"
    7. Cada indicador deve ter um ● colorido (verde/vermelho/amarelo) — não cinza "Verificando..."
    8. Verificar que NÃO há botões de ação no painel (apenas texto de status)
    9. Fazer logout e confirmar que a aba "Status" desaparece
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/previsao/atual | Chamada GET sem parâmetros de usuário; retorna dados públicos de previsão |
| Session → state.usuario | calendar_connected/calendar_disconnected lidos de payload de sessão já validado pelo backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Information Disclosure | #tab-status visibilidade | mitigate | Botão hidden por default (`style="display:none"`); carregarSessao() só exibe para usuario autenticado — mesmo padrão do Admin já em produção |
| T-09-01-02 | Spoofing | state.usuario.calendar_connected | accept | Campo vem de sessão server-side já autenticada; não há input de usuário neste painel |
| T-09-01-03 | Information Disclosure | carregarStatus() expõe estado de push | accept | Dado local ao browser (Notification.permission); não trafega pela rede |
</threat_model>

<verification>
Após execução, confirmar:
1. `grep "tab-btn-status" frontend/index.html frontend/src/main.js` — aparece em ambos os arquivos
2. `grep "carregarStatus" frontend/src/main.js` — aparece na definição e no tab click handler
3. `grep "status-indicador" frontend/src/styles/main.css` — regra CSS presente
4. Teste visual: login → aba Status visível → 3 indicadores coloridos → logout → aba desaparece
</verification>

<success_criteria>
- Botão "Status" aparece na sidebar para usuário autenticado, entre Calendário e Admin (per D-06)
- Painel exibe três indicadores com ● colorido e texto descritivo (per D-04)
- Indicador de Previsão fica vermelho quando stale (>120 min) e verde quando atualizado (per D-04)
- Indicador de Calendário usa calendar_connected/calendar_disconnected de state.usuario (per D-04, pitfall 7 evitado)
- Indicador de Push consulta getSubscription() em tempo real (per D-04, pitfall 4 evitado)
- Nenhum botão de ação inline no painel (per D-05)
- Aba invisível para usuário não autenticado (per D-03)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/09-frontend-ux/09-01-SUMMARY.md` seguindo o template em `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
