---
phase: 10-onboarding-wizard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/config/database.js
  - backend/src/routes/auth.js
  - backend/src/routes/usuarios.js
  - backend/src/app.js
  - frontend/src/services/api.js
autonomous: true
requirements:
  - UX-04

must_haves:
  truths:
    - "Após migration, SELECT na tabela usuarios retorna coluna onboarding_done com valor 0 para usuários existentes"
    - "GET /api/auth/me retorna campo onboarding_done no JSON de resposta"
    - "PATCH /api/usuarios/me com { onboarding_done: 1 } grava o flag e retorna { ok: true }"
    - "PATCH /api/usuarios/me sem autenticação retorna 401"
    - "api.usuarios.setOnboardingDone() existe e chama PATCH /api/usuarios/me"
  artifacts:
    - path: "backend/src/routes/usuarios.js"
      provides: "Router Express com PATCH /me protegido por requireAuth"
      exports: ["router (module.exports)"]
    - path: "backend/src/config/database.js"
      provides: "Migration segura de onboarding_done"
      contains: "ALTER TABLE usuarios ADD COLUMN onboarding_done INTEGER NOT NULL DEFAULT 0"
    - path: "backend/src/routes/auth.js"
      provides: "Endpoint GET /me com onboarding_done no SELECT e na resposta JSON"
      contains: "onboarding_done"
    - path: "backend/src/app.js"
      provides: "Mount do usuariosRouter em /api/usuarios"
      contains: "app.use('/api/usuarios'"
    - path: "frontend/src/services/api.js"
      provides: "Método api.usuarios.setOnboardingDone()"
      contains: "setOnboardingDone"
  key_links:
    - from: "frontend/src/services/api.js"
      to: "PATCH /api/usuarios/me"
      via: "api.usuarios.setOnboardingDone()"
      pattern: "setOnboardingDone.*PATCH.*usuarios/me"
    - from: "backend/src/routes/usuarios.js"
      to: "tabela usuarios (campo onboarding_done)"
      via: "db.run UPDATE WHERE id = req.session.userId"
      pattern: "UPDATE usuarios SET onboarding_done"

threat_model:
  trust_boundaries:
    - boundary: "cliente → PATCH /api/usuarios/me"
      description: "Usuário autenticado envia { onboarding_done: 1 }; sem autenticação a rota deve rejeitar com 401"

  stride_register:
    - id: "T-10-01"
      category: "Spoofing / Elevation of Privilege"
      component: "PATCH /api/usuarios/me"
      disposition: "mitigate"
      mitigation: "requireAuth middleware verifica req.session.userId; UPDATE usa WHERE id = req.session.userId — nunca req.body.userId"
    - id: "T-10-02"
      category: "Tampering"
      component: "PATCH /api/usuarios/me — corpo da requisição"
      disposition: "mitigate"
      mitigation: "Validar que req.body.onboarding_done === 1 (único valor aceito); retornar 400 para qualquer outro valor"
    - id: "T-10-03"
      category: "Information Disclosure"
      component: "GET /api/auth/me — exposição de onboarding_done"
      disposition: "accept"
      mitigation: "Campo é um inteiro binário (0/1) sem informação sensível; rota já exige sessão válida"
---

<objective>
Implementa a camada backend completa do wizard de onboarding: migration segura da coluna
`onboarding_done` no banco, exposição do campo no endpoint de sessão, criação do endpoint
PATCH para gravar o flag, e o método correspondente no cliente de API do frontend.

Purpose: Sem esse plano, o frontend não tem como saber se o usuário precisa ver o wizard
(onboarding_done ausente do /api/auth/me) nem como gravar que o wizard foi concluído
(PATCH /api/usuarios/me não existe). O plano 10-02 depende inteiramente destes contratos.

Output:
- backend/src/config/database.js — migration onboarding_done
- backend/src/routes/auth.js — onboarding_done no SELECT + JSON de resposta
- backend/src/routes/usuarios.js — criado do zero com PATCH /me
- backend/src/app.js — mount do novo router
- frontend/src/services/api.js — api.usuarios.setOnboardingDone()
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-onboarding-wizard/10-CONTEXT.md
@.planning/phases/10-onboarding-wizard/10-RESEARCH.md

<interfaces>
<!-- Contratos existentes que o executor DEVE respeitar. Extraídos diretamente do codebase. -->

De backend/src/config/database.js (linhas 191-203) — padrão de migration segura:
```javascript
// Padrão estabelecido — replicar exatamente
try {
  db.run(`ALTER TABLE usuarios ADD COLUMN alert_threshold INTEGER NOT NULL DEFAULT 51`);
} catch (_) {
  // Coluna já existe — ignorar silenciosamente
}

try {
  db.run(`ALTER TABLE usuarios ADD COLUMN alert_hours_before INTEGER NOT NULL DEFAULT 24`);
} catch (_) {
  // Coluna já existe — ignorar silenciosamente
}
// ← INSERIR BLOCO onboarding_done AQUI (linha 204, antes de }  module.exports)
```

De backend/src/routes/auth.js (linhas 111-130) — endpoint /api/auth/me atual:
```javascript
router.get('/me', (req, res) => {
  if (!req.session?.userId) {
    return res.status(401).json({ erro: 'Não autenticado' });
  }
  const db = getDb();
  const usuario = db.get(
    `SELECT calendar_connected, calendar_disconnected, alert_threshold, alert_hours_before FROM usuarios WHERE id = ?`,
    [req.session.userId]
  );
  res.json({
    id:                    req.session.userId,
    email:                 req.session.email,
    nome:                  req.session.nome,
    calendar_connected:    usuario?.calendar_connected    ?? 0,
    calendar_disconnected: usuario?.calendar_disconnected ?? 0,
    alert_threshold:       usuario?.alert_threshold       ?? 51,
    alert_hours_before:    usuario?.alert_hours_before    ?? 24,
  });
});
```

De backend/src/routes/push.js (linhas 60-80) — padrão PATCH para copiar em usuarios.js:
```javascript
router.patch('/threshold', requireAuth, (req, res) => {
  const { threshold } = req.body;
  // validação...
  const db = getDb();
  try {
    db.run(`UPDATE usuarios SET alert_threshold = ?, atualizado_em = datetime('now') WHERE id = ?`,
      [threshold, req.session.userId]);
    res.json({ ok: true });
  } catch (err) {
    console.error('[push] Erro ao atualizar threshold:', err.message);
    res.status(500).json({ erro: 'Erro interno' });
  }
});
```

De backend/src/app.js (linhas 28-77) — padrão de mount de routers:
```javascript
const authRouter   = require('./routes/auth');
const pushRouter        = require('./routes/push');
// ...
app.use('/api/push',        pushRouter);
// ← ADICIONAR: const usuariosRouter = require('./routes/usuarios');
// ← ADICIONAR: app.use('/api/usuarios', usuariosRouter);
```

De frontend/src/services/api.js (linhas 61-71) — padrão de sub-objeto (api.push):
```javascript
push: {
  subscribe: (subscriptionJSON) =>
    request('/push/subscribe', { method: 'POST', body: JSON.stringify(subscriptionJSON) }),
  setThreshold: (threshold) =>
    request('/push/threshold', { method: 'PATCH', body: JSON.stringify({ threshold }) }),
  // ...
},
// ← ADICIONAR: usuarios: { setOnboardingDone: () => ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Migration segura + exposição em /api/auth/me</name>
  <files>backend/src/config/database.js, backend/src/routes/auth.js</files>

  <read_first>
    - backend/src/config/database.js — para ver o estado atual e inserir após a linha do bloco alert_hours_before (linha ~203)
    - backend/src/routes/auth.js — para ver o SELECT atual (linha 117) e o objeto JSON retornado (linhas 121-129)
  </read_first>

  <action>
**Em backend/src/config/database.js:**

Adicionar o bloco abaixo imediatamente após o bloco try/catch de `alert_hours_before`
(que termina na linha ~203, antes do fechamento da função `initSchema`):

```javascript
// Adicionar onboarding_done se não existir — flag de wizard de onboarding (UX-04)
try {
  db.run(`ALTER TABLE usuarios ADD COLUMN onboarding_done INTEGER NOT NULL DEFAULT 0`);
} catch (_) {
  // Coluna já existe — ignorar silenciosamente
}
```

**Em backend/src/routes/auth.js:**

1. Expandir o SELECT na linha 117 de:
   ```
   `SELECT calendar_connected, calendar_disconnected, alert_threshold, alert_hours_before FROM usuarios WHERE id = ?`
   ```
   para:
   ```
   `SELECT calendar_connected, calendar_disconnected, alert_threshold, alert_hours_before, onboarding_done FROM usuarios WHERE id = ?`
   ```

2. Adicionar a linha `onboarding_done: usuario?.onboarding_done ?? 0,` no objeto JSON
   retornado por `res.json({...})`, após a linha `alert_hours_before`:
   ```javascript
   res.json({
     id:                    req.session.userId,
     email:                 req.session.email,
     nome:                  req.session.nome,
     calendar_connected:    usuario?.calendar_connected    ?? 0,
     calendar_disconnected: usuario?.calendar_disconnected ?? 0,
     alert_threshold:       usuario?.alert_threshold       ?? 51,
     alert_hours_before:    usuario?.alert_hours_before    ?? 24,
     onboarding_done:       usuario?.onboarding_done       ?? 0,
   });
   ```
  </action>

  <verify>
    <automated>
      node -e "const {getDb} = require('./backend/src/config/database.js'); const db = getDb(); const col = db.get(\"SELECT * FROM pragma_table_info('usuarios') WHERE name='onboarding_done'\"); if (!col) throw new Error('coluna onboarding_done ausente'); console.log('OK:', col);"
    </automated>
  </verify>

  <acceptance_criteria>
    - backend/src/config/database.js contém a string `ALTER TABLE usuarios ADD COLUMN onboarding_done INTEGER NOT NULL DEFAULT 0`
    - backend/src/routes/auth.js linha do SELECT contém `onboarding_done` entre as colunas selecionadas
    - backend/src/routes/auth.js objeto `res.json({...})` contém `onboarding_done: usuario?.onboarding_done ?? 0`
  </acceptance_criteria>

  <done>Migration executa sem erro em banco existente; GET /api/auth/me retorna onboarding_done no JSON.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Criar usuarios.js + montar em app.js</name>
  <files>backend/src/routes/usuarios.js, backend/src/app.js</files>

  <read_first>
    - backend/src/routes/push.js — para ler o padrão completo de um router PATCH com requireAuth (linhas 1-10 para os requires, linhas 60-80 para o handler PATCH)
    - backend/src/middleware/auth.js — para confirmar a assinatura do middleware requireAuth
    - backend/src/app.js — para ver o estado atual e onde inserir o require + app.use
  </read_first>

  <action>
**Criar backend/src/routes/usuarios.js** do zero com o seguinte conteúdo completo:

```javascript
const express       = require('express');
const router        = express.Router();
const { getDb }     = require('../config/database');
const requireAuth   = require('../middleware/auth');

// PATCH /api/usuarios/me — grava onboarding_done = 1 (UX-04, D-06, D-07)
// Requer sessão autenticada. Aceita apenas { onboarding_done: 1 }.
// Endpoint idempotente: chamar múltiplas vezes é seguro.
router.patch('/me', requireAuth, (req, res) => {
  const { onboarding_done } = req.body;
  if (onboarding_done !== 1) {
    return res.status(400).json({ erro: 'onboarding_done deve ser 1' });
  }
  const db = getDb();
  try {
    db.run(
      `UPDATE usuarios SET onboarding_done = 1, atualizado_em = datetime('now') WHERE id = ?`,
      [req.session.userId]
    );
    res.json({ ok: true });
  } catch (err) {
    console.error('[usuarios] Erro ao atualizar onboarding_done:', err.message);
    res.status(500).json({ erro: 'Erro interno' });
  }
});

module.exports = router;
```

**Em backend/src/app.js:**

1. Adicionar o require do usuariosRouter logo após o `require('./routes/admin')` (linha ~36):
   ```javascript
   const usuariosRouter   = require('./routes/usuarios');
   ```

2. Adicionar o mount logo após `app.use('/api/admin', adminRouter)` (linha ~77):
   ```javascript
   app.use('/api/usuarios', usuariosRouter);
   ```
  </action>

  <verify>
    <automated>
      node -e "const app = require('./backend/src/app.js'); console.log('app carregado OK');" 2>&1 | head -5
    </automated>
  </verify>

  <acceptance_criteria>
    - Arquivo backend/src/routes/usuarios.js existe
    - backend/src/routes/usuarios.js contém `router.patch('/me', requireAuth`
    - backend/src/routes/usuarios.js contém `UPDATE usuarios SET onboarding_done = 1`
    - backend/src/routes/usuarios.js contém `WHERE id = ?` com `req.session.userId` (nunca req.body.userId)
    - backend/src/routes/usuarios.js contém `if (onboarding_done !== 1)` com retorno 400
    - backend/src/app.js contém `require('./routes/usuarios')`
    - backend/src/app.js contém `app.use('/api/usuarios', usuariosRouter)`
  </acceptance_criteria>

  <done>PATCH /api/usuarios/me retorna 401 sem sessão, 400 com onboarding_done != 1, e { ok: true } com dado correto.</done>
</task>

<task type="auto">
  <name>Tarefa 3: Adicionar api.usuarios.setOnboardingDone() em api.js</name>
  <files>frontend/src/services/api.js</files>

  <read_first>
    - frontend/src/services/api.js — para ver o estado atual do objeto `api` e onde inserir o novo sub-objeto `usuarios`
  </read_first>

  <action>
**Em frontend/src/services/api.js:**

Adicionar o sub-objeto `usuarios` dentro do objeto `api` exportado, imediatamente após
o fechamento do sub-objeto `push` (após a linha que fecha `push: { ... },`) e antes
do sub-objeto `alertas`:

```javascript
  usuarios: {
    // Grava onboarding_done = 1. Chamado ao pular ou concluir o wizard (UX-04, D-06, D-07).
    setOnboardingDone: () =>
      request('/usuarios/me', { method: 'PATCH', body: JSON.stringify({ onboarding_done: 1 }) }),
  },
```

O resultado final na região relevante de api.js deve ser:
```javascript
  // ... push: { ... },

  usuarios: {
    setOnboardingDone: () =>
      request('/usuarios/me', { method: 'PATCH', body: JSON.stringify({ onboarding_done: 1 }) }),
  },

  alertas: {
    // ... conteúdo existente ...
  },
```
  </action>

  <verify>
    <automated>
      grep -n "setOnboardingDone" "frontend/src/services/api.js"
    </automated>
  </verify>

  <acceptance_criteria>
    - frontend/src/services/api.js contém `setOnboardingDone`
    - frontend/src/services/api.js contém `PATCH` e `/usuarios/me` na mesma linha ou bloco do método
    - frontend/src/services/api.js contém `onboarding_done: 1` no JSON.stringify do método
    - O novo sub-objeto está dentro do export `api` (não fora do objeto)
  </acceptance_criteria>

  <done>api.usuarios.setOnboardingDone() existe e envia PATCH /api/usuarios/me com { onboarding_done: 1 }.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → PATCH /api/usuarios/me | Usuário autenticado envia corpo JSON; sem sessão deve ser rejeitado com 401 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Spoofing / Elevation of Privilege | PATCH /api/usuarios/me | mitigate | requireAuth verifica req.session.userId; UPDATE usa WHERE id = req.session.userId — nunca req.body.userId |
| T-10-02 | Tampering | Corpo do PATCH | mitigate | Validar que req.body.onboarding_done === 1 (strict equality); retornar 400 para qualquer outro valor |
| T-10-03 | Information Disclosure | GET /api/auth/me (exposição de onboarding_done) | accept | Campo é inteiro binário (0/1) sem informação sensível; rota já exige sessão autenticada |
</threat_model>

<verification>
Verificações end-to-end após execução completa deste plano:

1. `grep "onboarding_done" backend/src/config/database.js` — deve retornar a linha do ALTER TABLE
2. `grep "onboarding_done" backend/src/routes/auth.js` — deve retornar 2 linhas (SELECT + res.json)
3. `test -f backend/src/routes/usuarios.js && echo OK` — arquivo existe
4. `grep "app.use.*api/usuarios" backend/src/app.js` — mount presente
5. `grep "setOnboardingDone" frontend/src/services/api.js` — método presente
6. Servidor sobe sem erro: `node backend/src/app.js &` (verificar que não há crash de startup)
</verification>

<success_criteria>
- Migration onboarding_done está no banco após startup (padrão try/catch idêntico ao alert_threshold)
- GET /api/auth/me retorna { ..., onboarding_done: 0 } para usuários existentes
- PATCH /api/usuarios/me com sessão válida e { onboarding_done: 1 } → 200 { ok: true }
- PATCH /api/usuarios/me sem sessão → 401
- PATCH /api/usuarios/me com { onboarding_done: 0 } → 400
- api.usuarios.setOnboardingDone() disponível no frontend
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/10-onboarding-wizard/10-01-SUMMARY.md` seguindo o
template padrão em @$HOME/.claude/get-shit-done/templates/summary.md.
</output>
