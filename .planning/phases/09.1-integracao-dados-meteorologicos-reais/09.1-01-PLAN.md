---
phase: 09.1-integracao-dados-meteorologicos-reais
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/config/database.js
  - backend/.env
  - backend/.env.example
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-06
  - D-07
  - D-08
  - D-09

must_haves:
  truths:
    - "A tabela weather_observations existe no banco SQLite após reinicialização do backend"
    - "Multiplas fontes podem registrar dados para o mesmo timestamp sem conflito UNIQUE"
    - "As tabelas forecasts e forecasts_meta permanecem intactas"
    - "VISUAL_CROSSING_API_KEY e REDEMET_API_KEY aparecem no .env.example com comentarios de como obter"
  artifacts:
    - path: "backend/src/config/database.js"
      provides: "DDL CREATE TABLE weather_observations + CREATE INDEX idx_weather_obs_bairro_time em initSchema()"
      contains: "CREATE TABLE IF NOT EXISTS weather_observations"
    - path: "backend/.env.example"
      provides: "Documentacao das novas variaveis de ambiente"
      contains: "VISUAL_CROSSING_API_KEY="
    - path: "backend/.env"
      provides: "Variaveis em branco para desenvolvimento local (graceful degradation D-08/D-09)"
      contains: "VISUAL_CROSSING_API_KEY="
  key_links:
    - from: "backend/src/config/database.js initSchema()"
      to: "weather_observations table"
      via: "CREATE TABLE IF NOT EXISTS — idempotente; seguro em bancos existentes"
      pattern: "CREATE TABLE IF NOT EXISTS weather_observations"
---

<objective>
Preparar o schema SQLite e as variaveis de ambiente necessarios para os dois novos servicos de dados meteorologicos.

Purpose: Plans 02 e 03 (visualCrossingService e redemetService) dependem de weather_observations existir no banco antes de tentar INSERT. Esta e a unica preparacao de infraestrutura necessaria — zero dependencias de runtime externas.

Output: weather_observations DDL em initSchema() + entradas de variaveis de ambiente com comentarios de instrucao para o usuario.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.1-integracao-dados-meteorologicos-reais/09.1-CONTEXT.md
@.planning/phases/09.1-integracao-dados-meteorologicos-reais/09.1-PATTERNS.md

<interfaces>
<!-- Existing database.js structure — executor must know the exact insertion point -->

Ponto de insercao em initSchema():
- Linha 169: db.run CREATE INDEX idx_alertas_enviados_usuario_em ON alertas_enviados(usuario_id, enviado_em)
- Linha 173: fechamento do bloco db.run (ultimo parentese)
- Linha 174+: comentario "// Adicionar alert_threshold" seguido de try/catch ALTER TABLE

Inserir o DDL APOS a linha 173 e ANTES do comentario "// Adicionar alert_threshold".

Tabelas existentes que NAO devem ser tocadas:
- forecasts: UNIQUE(bairro, forecast_time) — intocavel (D-03)
- forecasts_meta: row id=1 sempre existe; tem precip_48h_mm REAL NOT NULL DEFAULT 0

DDL exato a inserir (D-02):
```sql
CREATE TABLE IF NOT EXISTS weather_observations (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  bairro          TEXT    NOT NULL DEFAULT 'florianopolis',
  observed_time   TEXT    NOT NULL,
  precipitacao_mm REAL    NOT NULL DEFAULT 0,
  fonte           TEXT    NOT NULL,
  fetched_at      TEXT    NOT NULL DEFAULT (datetime('now')),
  UNIQUE(bairro, observed_time, fonte)
)

CREATE INDEX IF NOT EXISTS idx_weather_obs_bairro_time
  ON weather_observations(bairro, observed_time)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar DDL weather_observations em database.js</name>
  <files>backend/src/config/database.js</files>
  <read_first>
    - backend/src/config/database.js — ler o arquivo completo para confirmar a estrutura atual e localizar o ponto exato de insercao (linha 172: fechamento do bloco idx_alertas_enviados_usuario_em)
    - .planning/phases/09.1-integracao-dados-meteorologicos-reais/09.1-PATTERNS.md — secao "File 3: database.js (MODIFY)" para confirmar o DDL e posicao
  </read_first>
  <action>
No arquivo backend/src/config/database.js, localizar o bloco `db.run(...)` que cria o indice `idx_alertas_enviados_usuario_em` (linhas 169-172). Inserir exatamente APOS o fechamento desse db.run e ANTES do comentario `// Adicionar alert_threshold` (linha 174).

Inserir este bloco completo (dois db.run separados):

```javascript
  db.run(`
    CREATE TABLE IF NOT EXISTS weather_observations (
      id              INTEGER PRIMARY KEY AUTOINCREMENT,
      bairro          TEXT    NOT NULL DEFAULT 'florianopolis',
      observed_time   TEXT    NOT NULL,
      precipitacao_mm REAL    NOT NULL DEFAULT 0,
      fonte           TEXT    NOT NULL,
      fetched_at      TEXT    NOT NULL DEFAULT (datetime('now')),
      UNIQUE(bairro, observed_time, fonte)
    )
  `);

  db.run(`
    CREATE INDEX IF NOT EXISTS idx_weather_obs_bairro_time
      ON weather_observations(bairro, observed_time)
  `);
```

Regras criticas:
- NAO alterar nenhuma tabela existente (forecasts, forecasts_meta, risk_scores, usuarios, alertas_enviados, etc.)
- NAO alterar o UNIQUE constraint de forecasts — permanece UNIQUE(bairro, forecast_time)
- O DDL usa CREATE TABLE IF NOT EXISTS — idempotente e seguro em bancos existentes
- O indice usa CREATE INDEX IF NOT EXISTS — igualmente idempotente
- NAO adicionar coluna tipo_qualitativo na tabela — logs de tipo sao feitos via console.log pelo redemetService, nao persistidos
  </action>
  <verify>
    <automated>cd /c/Users/arthu/floripa-alagamentos && node -e "const { getDb } = require('./backend/src/config/database'); const db = getDb(); const row = db.get(\"SELECT name FROM sqlite_master WHERE type='table' AND name='weather_observations'\"); console.log(row ? 'PASS: weather_observations exists' : 'FAIL: table not found');"</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/config/database.js contem a string `CREATE TABLE IF NOT EXISTS weather_observations`
    - backend/src/config/database.js contem `UNIQUE(bairro, observed_time, fonte)`
    - backend/src/config/database.js contem `CREATE INDEX IF NOT EXISTS idx_weather_obs_bairro_time`
    - backend/src/config/database.js contem `ON weather_observations(bairro, observed_time)`
    - backend/src/config/database.js NAO tem alteracoes nas linhas 58-86 (tabelas forecasts/forecasts_meta — verificar que CREATE TABLE forecasts ainda tem UNIQUE(bairro, forecast_time))
    - O bloco novo esta posicionado apos o indice `idx_alertas_enviados_usuario_em` e antes do bloco `try { db.run` de ALTER TABLE
    - Executar `node -e "require('./backend/src/config/database').getDb()"` no diretorio raiz sem erros de sintaxe ou runtime
  </acceptance_criteria>
  <done>weather_observations e idx_weather_obs_bairro_time existem no SQLite apos qualquer chamada a getDb(). Demais tabelas intactas e funcionais.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar variaveis de ambiente ao .env e .env.example</name>
  <files>backend/.env, backend/.env.example</files>
  <read_first>
    - backend/.env.example — ler o arquivo completo (21 linhas) para confirmar o conteudo atual e a ultima linha (NODE_ENV=development)
    - backend/.env — ler o arquivo completo (12 linhas) para confirmar o conteudo e a ultima linha (VAPID_PRIVATE_KEY=...)
    - .planning/phases/09.1-integracao-dados-meteorologicos-reais/09.1-PATTERNS.md — secao "File 6: .env e .env.example (MODIFY)" para o texto exato a adicionar
  </read_first>
  <action>
**No arquivo backend/.env.example**, adicionar ao final (apos a linha `NODE_ENV=development`):

```
# Weather data APIs — optional; graceful degradation if absent
# Visual Crossing Timeline API — observed precipitation (mm) last 24h
# Sign up at https://www.visualcrossing.com/sign-up (free tier: 1,000 records/day)
VISUAL_CROSSING_API_KEY=

# REDEMET API — METAR aeronautical observations SBFL (qualitative only, no mm)
# Request access at https://redemet.decea.mil.br/api.html
REDEMET_API_KEY=
```

**No arquivo backend/.env**, adicionar ao final (apos a ultima linha VAPID_PRIVATE_KEY=...):

```
VISUAL_CROSSING_API_KEY=
REDEMET_API_KEY=
```

Regras criticas:
- NAO preencher nenhum valor real nas chaves — deixar em branco (graceful degradation D-08/D-09 garante operacao sem elas)
- NAO alterar nenhuma variavel existente nos arquivos
- NAO logar o valor real de VISUAL_CROSSING_API_KEY ou REDEMET_API_KEY em nenhum arquivo — apenas `[key presente]` ou `[key ausente]`
- .env esta no .gitignore — nao ha risco de vazar segredos
  </action>
  <verify>
    <automated>grep "VISUAL_CROSSING_API_KEY=" /c/Users/arthu/floripa-alagamentos/backend/.env.example && grep "REDEMET_API_KEY=" /c/Users/arthu/floripa-alagamentos/backend/.env.example && grep "VISUAL_CROSSING_API_KEY=" /c/Users/arthu/floripa-alagamentos/backend/.env && grep "REDEMET_API_KEY=" /c/Users/arthu/floripa-alagamentos/backend/.env && echo "PASS: all env vars present"</automated>
  </verify>
  <acceptance_criteria>
    - backend/.env.example contem `VISUAL_CROSSING_API_KEY=`
    - backend/.env.example contem `REDEMET_API_KEY=`
    - backend/.env.example contem o comentario `# Sign up at https://www.visualcrossing.com/sign-up`
    - backend/.env.example contem o comentario `# Request access at https://redemet.decea.mil.br`
    - backend/.env contem `VISUAL_CROSSING_API_KEY=` (valor em branco)
    - backend/.env contem `REDEMET_API_KEY=` (valor em branco)
    - Nenhuma chave API real aparece no .env.example
    - As variaveis existentes (GOOGLE_CLIENT_ID, SESSION_SECRET, VAPID_*, etc.) continuam intactas
  </acceptance_criteria>
  <done>As duas chaves de API estao declaradas em .env (graceful-skip quando vazias) e documentadas em .env.example com instrucoes de obtencao.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env → process.env | Credenciais lidas do filesystem para memoria do processo; nao expostas ao cliente |
| database.js → SQLite | Writes via parametros preparados (db.run com array de parametros) — sem concatenacao de strings no DDL novo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-01-01 | Information Disclosure | .env VISUAL_CROSSING_API_KEY / REDEMET_API_KEY | mitigate | Valores em branco no .env de desenvolvimento; .env listado no .gitignore; nunca logar o valor da chave — apenas presenca/ausencia via `!process.env.KEY` |
| T-09.1-01-02 | Tampering | database.js DDL initSchema() | accept | DDL e string literal estatica sem interpolacao de variaveis externas; CREATE TABLE IF NOT EXISTS e idempotente; nao ha input de usuario neste plano |
| T-09.1-01-03 | Information Disclosure | .env.example publicado no repo | accept | .env.example contem apenas chaves em branco e URLs publicas de cadastro; nenhuma credencial real; risco zero |
</threat_model>

<verification>
Apos execucao dos dois tasks:
1. `node -e "require('./backend/src/config/database').getDb()"` — sem erros
2. `grep "weather_observations" backend/src/config/database.js` — retorna pelo menos 3 linhas (CREATE TABLE, UNIQUE, CREATE INDEX)
3. `grep "VISUAL_CROSSING_API_KEY" backend/.env backend/.env.example` — retorna 2 linhas (uma por arquivo)
4. `grep "REDEMET_API_KEY" backend/.env backend/.env.example` — retorna 2 linhas
5. `grep "UNIQUE(bairro, forecast_time)" backend/src/config/database.js` — retorna a linha original da tabela forecasts (intacta)
</verification>

<success_criteria>
- weather_observations table criada com UNIQUE(bairro, observed_time, fonte) — suporta multiplas fontes por timestamp
- idx_weather_obs_bairro_time criado — queries por bairro+tempo eficientes
- forecasts e forecasts_meta intactas — Open-Meteo continua funcionando sem interrupcao
- VISUAL_CROSSING_API_KEY e REDEMET_API_KEY presentes no .env (em branco) e documentadas no .env.example
- Backend inicia sem erros com as novas entradas de schema
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/09.1-integracao-dados-meteorologicos-reais/09.1-01-SUMMARY.md`
</output>
