---
phase: 09.1-integracao-dados-meteorologicos-reais
plan: 04
type: execute
wave: 3
depends_on:
  - 09.1-02
  - 09.1-03
files_modified:
  - backend/src/jobs/scheduler.js
autonomous: true
requirements:
  - D-15
  - D-16

must_haves:
  truths:
    - "Todo ciclo horario executa: forecastService → visualCrossingService → redemetService (nessa ordem)"
    - "No startup do backend os tres fetches rodam na sequencia antes de calcularRiscos"
    - "Falha em qualquer um dos novos fetches nao impede calcularRiscos ou checkAndSendAlerts de rodar"
    - "O log de inicializacao do scheduler reflete os tres servicos no cron horario"
  artifacts:
    - path: "backend/src/jobs/scheduler.js"
      provides: "Scheduler atualizado com imports e chamadas para visualCrossingService e redemetService"
      contains: "fetchVisualCrossing"
  key_links:
    - from: "scheduler.js cron '0 * * * *'"
      to: "fetchVisualCrossing() + fetchRedemet()"
      via: "await em sequencia apos fetchAndCacheForecasts()"
      pattern: "await fetchVisualCrossing"
    - from: "scheduler.js IIFE startup"
      to: "fetchVisualCrossing() + fetchRedemet()"
      via: "try/catch blocks entre forecastService e calcularRiscos"
      pattern: "await fetchVisualCrossing"
---

<objective>
Registrar visualCrossingService e redemetService no scheduler.js existente — adicionando imports, chamadas no cron horario e chamadas no IIFE de startup na ordem correta.

Purpose: Plans 02 e 03 criaram os servicos mas eles nunca serao chamados ate serem registrados no scheduler. Esta e a ligacao final que ativa os novos fetches no ciclo de producao.

Output: scheduler.js modificado com os dois novos servicos integrados. Nenhum novo arquivo criado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09.1-integracao-dados-meteorologicos-reais/09.1-CONTEXT.md
@.planning/phases/09.1-integracao-dados-meteorologicos-reais/09.1-PATTERNS.md

<interfaces>
<!-- Exact current state of scheduler.js — executor must know what to change -->

Estrutura atual de scheduler.js (76 linhas):

IMPORTS (linhas 8-12):
```javascript
const cron = require('node-cron');
const { fetchAndCacheForecasts } = require('../services/forecastService');
const { calcularRiscos } = require('../services/riskEngine');
const { syncAllConnectedUsers } = require('../services/calendarService');
const { checkAndSendAlerts } = require('../services/alertService');
```

CRON HORARIO (linhas 29-34):
```javascript
cron.schedule('0 * * * *', async () => {
  console.log('[scheduler] Executando fetch horario de previsao');
  await fetchAndCacheForecasts();
}, {
  timezone: 'America/Sao_Paulo',
});
```

LOG DE INICIALIZACAO (linha 52):
```javascript
console.log('[scheduler] Jobs iniciados. Forecast: 0 * * * * | Risco+Alertas: 5 */4 * * * | Calendar: */30 * * * *');
```

IIFE DE STARTUP (linhas 56-72):
```javascript
(async () => {
  try {
    await fetchAndCacheForecasts();
  } catch (err) {
    console.error('[scheduler] Erro na busca inicial de forecast:', err.message);
  }
  try {
    await calcularRiscos();
  } catch (err) {
    console.error('[scheduler] Erro no calculo inicial de risco:', err.message);
  }
  try {
    await checkAndSendAlerts();
  } catch (err) {
    console.error('[scheduler] Erro na verificacao inicial de alertas:', err.message);
  }
})();
```

TRES MUDANCAS NECESSARIAS:
1. Adicionar 2 imports apos linha 12
2. Adicionar 2 awaits no cron horario (linhas 29-34)
3. Adicionar 2 try/catch blocks no IIFE (entre forecastService e calcularRiscos)
4. Atualizar string do console.log na linha 52
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrar novos servicos no scheduler.js</name>
  <files>backend/src/jobs/scheduler.js</files>
  <read_first>
    - backend/src/jobs/scheduler.js — ler o arquivo completo para confirmar a estrutura atual exata (76 linhas) e os tres pontos de insercao antes de fazer qualquer modificacao
    - .planning/phases/09.1-integracao-dados-meteorologicos-reais/09.1-PATTERNS.md — secao "File 4: scheduler.js (MODIFY)" com os tres pontos de insercao documentados com codigo exato
  </read_first>
  <action>
No arquivo backend/src/jobs/scheduler.js, realizar TRES modificacoes cirurgicas:

**MODIFICACAO 1 — Adicionar imports (apos linha 12, apos o require de alertService):**

Inserir estas duas linhas logo apos `const { checkAndSendAlerts } = require('../services/alertService');`:

```javascript
const { fetchAndCacheObservations: fetchVisualCrossing } = require('../services/visualCrossingService');
const { fetchAndCacheObservations: fetchRedemet } = require('../services/redemetService');
```

Os aliases `fetchVisualCrossing` e `fetchRedemet` evitam conflito de nome com `fetchAndCacheForecasts`.

**MODIFICACAO 2 — Adicionar chamadas no cron horario (linhas 29-34):**

Substituir o bloco do cron '0 * * * *' de:
```javascript
  cron.schedule('0 * * * *', async () => {
    console.log('[scheduler] Executando fetch horario de previsao');
    await fetchAndCacheForecasts();
  }, {
    timezone: 'America/Sao_Paulo',
  });
```

Por:
```javascript
  cron.schedule('0 * * * *', async () => {
    console.log('[scheduler] Executando fetch horario de previsao');
    await fetchAndCacheForecasts();
    await fetchVisualCrossing();
    await fetchRedemet();
  }, {
    timezone: 'America/Sao_Paulo',
  });
```

Ordem D-15: forecastService → visualCrossingService → redemetService. calcularRiscos roda no cron de 4h separado — nao neste bloco.

**MODIFICACAO 3 — Adicionar try/catch no IIFE de startup (entre forecastService e calcularRiscos):**

Substituir a sequencia do IIFE de:
```javascript
    try {
      await fetchAndCacheForecasts();
    } catch (err) {
      console.error('[scheduler] Erro na busca inicial de forecast:', err.message);
    }
    try {
      await calcularRiscos();
    } catch (err) {
      console.error('[scheduler] Erro no calculo inicial de risco:', err.message);
    }
```

Por:
```javascript
    try {
      await fetchAndCacheForecasts();
    } catch (err) {
      console.error('[scheduler] Erro na busca inicial de forecast:', err.message);
    }
    try {
      await fetchVisualCrossing();
    } catch (err) {
      console.error('[scheduler] Erro no fetch inicial Visual Crossing:', err.message);
    }
    try {
      await fetchRedemet();
    } catch (err) {
      console.error('[scheduler] Erro no fetch inicial REDEMET:', err.message);
    }
    try {
      await calcularRiscos();
    } catch (err) {
      console.error('[scheduler] Erro no calculo inicial de risco:', err.message);
    }
```

Cada servico tem seu proprio try/catch — falha em um nao impede os seguintes (D-16).

**MODIFICACAO 4 — Atualizar string do console.log de inicializacao:**

Substituir:
```javascript
  console.log('[scheduler] Jobs iniciados. Forecast: 0 * * * * | Risco+Alertas: 5 */4 * * * | Calendar: */30 * * * *');
```

Por:
```javascript
  console.log('[scheduler] Jobs iniciados. Forecast+VC+REDEMET: 0 * * * * | Risco+Alertas: 5 */4 * * * | Calendar: */30 * * * *');
```

Regras criticas:
- NAO alterar o cron de risco/alertas (5 */4 * * *) nem o cron de calendario (*/30 * * * *)
- NAO alterar a logica de guard `if (initialized)` nem o flag `initialized = true`
- NAO alterar as chamadas a calcularRiscos() ou checkAndSendAlerts() — apenas inserir novos blocks antes de calcularRiscos no IIFE
- Os novos try/catch para VC e REDEMET devem ficar ENTRE forecastService e calcularRiscos (ordem exata per D-16)
  </action>
  <verify>
    <automated>node -e "require('./backend/src/jobs/scheduler'); console.log('PASS: scheduler loads without errors')" 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/jobs/scheduler.js contem `require('../services/visualCrossingService')`
    - backend/src/jobs/scheduler.js contem `require('../services/redemetService')`
    - backend/src/jobs/scheduler.js contem `fetchAndCacheObservations: fetchVisualCrossing` (alias no destructuring)
    - backend/src/jobs/scheduler.js contem `fetchAndCacheObservations: fetchRedemet` (alias no destructuring)
    - backend/src/jobs/scheduler.js contem `await fetchVisualCrossing()` em dois lugares (cron horario E IIFE startup)
    - backend/src/jobs/scheduler.js contem `await fetchRedemet()` em dois lugares (cron horario E IIFE startup)
    - backend/src/jobs/scheduler.js contem `Erro no fetch inicial Visual Crossing` (mensagem no try/catch do IIFE)
    - backend/src/jobs/scheduler.js contem `Erro no fetch inicial REDEMET` (mensagem no try/catch do IIFE)
    - backend/src/jobs/scheduler.js contem `Forecast+VC+REDEMET: 0 * * * *` no console.log de inicializacao
    - Os dois `await fetchVisualCrossing()` no IIFE aparecem ANTES de `await calcularRiscos()` (verificar com grep -n)
    - Os dois `await fetchRedemet()` no IIFE aparecem ANTES de `await calcularRiscos()` (verificar com grep -n)
    - `node -e "require('./backend/src/jobs/scheduler')"` retorna sem erros de sintaxe (pode logar msgs de cron mas nao deve ter SyntaxError)
  </acceptance_criteria>
  <done>scheduler.js atualizado com os tres servicos de fetch registrados. Ciclo horario: forecastService → visualCrossing → redemet. Startup IIFE: mesma ordem antes de calcularRiscos. Cada servico tem try/catch independente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scheduler.js → visualCrossingService | Chamada de funcao interna — sem novos vetores de ataque |
| scheduler.js → redemetService | Chamada de funcao interna — sem novos vetores de ataque |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-04-01 | Denial of Service | Erro em fetchVisualCrossing() ou fetchRedemet() no IIFE de startup | mitigate | Cada servico tem try/catch independente; erro em VC nao impede REDEMET de rodar; erro em ambos nao impede calcularRiscos(); padrão no-throw nos servicos (D-10) ja garante isso como segunda linha de defesa |
| T-09.1-04-02 | Tampering | require() carregando modulo errado se path estiver incorreto | mitigate | Paths relativos `../services/visualCrossingService` e `../services/redemetService` sao deterministicos; Node.js lanca erro explicito em require() se arquivo nao existir — facilmente identificado no startup |
| T-09.1-04-03 | Elevation of Privilege | Novo codigo no scheduler executando com privilegios do processo Node | accept | Scheduler ja roda com privilegios do processo; os dois novos servicos sao chamadas de funcao sincrona/async comuns sem escalada de privilegio; mesma superficie que forecastService existente |
</threat_model>

<verification>
Apos execucao do task:
1. `grep -n "fetchVisualCrossing\|fetchRedemet" backend/src/jobs/scheduler.js` — deve retornar pelo menos 6 linhas (2 imports, 2 no cron, 2 no IIFE)
2. `grep -n "await fetchVisualCrossing\|await fetchRedemet" backend/src/jobs/scheduler.js` — deve retornar 4 linhas (2 awaits no cron, 2 no IIFE)
3. `grep -n "calcularRiscos\|fetchVisualCrossing\|fetchRedemet" backend/src/jobs/scheduler.js` — verificar que no IIFE os dois novos awaits aparecem em linha MENOR que o await calcularRiscos()
4. `node -e "require('./backend/src/jobs/scheduler')"` — sem SyntaxError (pode logar avisos de key ausente dos novos servicos — isso e correto)
5. `grep "Forecast+VC+REDEMET" backend/src/jobs/scheduler.js` — retorna a linha do console.log atualizado
</verification>

<success_criteria>
- scheduler.js importa visualCrossingService e redemetService com aliases sem conflito
- Cron horario chama os tres fetches em ordem: forecastService → visualCrossing → redemet
- IIFE de startup chama os tres fetches em ordem antes de calcularRiscos
- Cada novo servico tem try/catch independente no IIFE
- Falha em VC ou REDEMET nao interrompe o ciclo de risco/alertas
- Backend inicia sem erros com as tres fontes registradas
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/09.1-integracao-dados-meteorologicos-reais/09.1-04-SUMMARY.md`
</output>
