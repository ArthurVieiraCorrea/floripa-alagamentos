---
phase: 10-onboarding-wizard
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - frontend/src/styles/main.css
  - frontend/src/main.js
autonomous: false
requirements:
  - UX-04

must_haves:
  truths:
    - "Novo usuário (onboarding_done === 0) vê o wizard modal ao fazer login, sem precisar encontrar configurações"
    - "Passo 1 é informativo; botão Próximo avança para o passo 2"
    - "Passo 2 tem botão 'Conectar Calendário' que chama api.conectarCalendario() com feedback inline; Próximo sempre habilitado"
    - "Passo 3 tem botão 'Ativar notificações' que reutiliza a lógica de btn-push-optin com feedback inline; Concluir grava o flag"
    - "Clicar 'Pular configuração' em qualquer passo chama api.usuarios.setOnboardingDone() e fecha o modal"
    - "Após concluir ou pular, recarregar a página não reabre o wizard (onboarding_done = 1 no banco)"
    - "Clicar fora do modal faz o modal tremer (shake animation) mas não fecha"
    - "Escape não fecha o modal"
    - "Tab circula apenas entre elementos focáveis do wizard (focus trap)"
  artifacts:
    - path: "frontend/src/styles/main.css"
      provides: "CSS completo do wizard: #wizard-backdrop, #wizard-modal, .wizard-progress, .wizard-dot, wizard-shake keyframes, botões, responsive"
      contains: "#wizard-backdrop"
    - path: "frontend/src/main.js"
      provides: "funções abrirWizard(), fecharWizard(), ativarPushNotificacoes(); disparo em carregarSessao()"
      contains: "abrirWizard"
  key_links:
    - from: "carregarSessao() em main.js"
      to: "abrirWizard()"
      via: "if (usuario.onboarding_done === 0 || !usuario.onboarding_done)"
      pattern: "onboarding_done.*abrirWizard"
    - from: "abrirWizard() passo 2"
      to: "api.conectarCalendario()"
      via: "click em btn-wizard-conectar-cal"
      pattern: "conectarCalendario"
    - from: "abrirWizard() passo 3"
      to: "ativarPushNotificacoes()"
      via: "click em btn-wizard-push"
      pattern: "ativarPushNotificacoes"
    - from: "wizard-btn-pular / wizard-btn-proximo (passo 3)"
      to: "api.usuarios.setOnboardingDone()"
      via: "await com try/catch silencioso"
      pattern: "setOnboardingDone"

threat_model:
  trust_boundaries:
    - boundary: "wizard frontend → PATCH /api/usuarios/me"
      description: "Chamada feita pelo wizard ao pular ou concluir; protegida pelo requireAuth no backend (plano 10-01)"

  stride_register:
    - id: "T-10-04"
      category: "Tampering"
      component: "HTML do modal (injeção por JS)"
      disposition: "accept"
      mitigation: "Copy é hardcoded em JS (não interpolado de user input); sem XSS possível neste fluxo"
    - id: "T-10-05"
      category: "Denial of Service"
      component: "Wizard re-abre em loop se PATCH falhar"
      disposition: "mitigate"
      mitigation: "Se PATCH falhar, fecharWizard() ainda é chamado (degradação silenciosa); wizard só reabre no próximo login, não em loop na mesma sessão"
---

<objective>
Implementa o wizard modal de 3 passos no frontend: CSS do modal, funções JS de abertura/fechamento,
lógica de cada passo (informativo, calendar connect, push optin), e disparo automático em
carregarSessao() quando onboarding_done === 0.

Purpose: Este é o artefato visível para o usuário — a experiência de onboarding propriamente dita.
Depende do plano 10-01 para ter o endpoint PATCH e o campo onboarding_done na sessão.

Output:
- frontend/src/styles/main.css — bloco CSS do wizard (UI-SPEC aprovada)
- frontend/src/main.js — abrirWizard(), fecharWizard(), ativarPushNotificacoes() + disparo em carregarSessao()
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-onboarding-wizard/10-CONTEXT.md
@.planning/phases/10-onboarding-wizard/10-RESEARCH.md
@.planning/phases/10-onboarding-wizard/10-UI-SPEC.md
@.planning/phases/10-onboarding-wizard/10-01-SUMMARY.md

<interfaces>
<!-- Contratos existentes que o executor DEVE respeitar. Extraídos diretamente do codebase. -->

De frontend/src/main.js linha 22 — variável de módulo swRegistration (acessível em abrirWizard):
```javascript
let swRegistration = null;
```

De frontend/src/main.js linhas 619-651 — carregarSessao() ATUAL (estado a modificar):
```javascript
async function carregarSessao() {
  try {
    const usuario = await api.sessao();
    const btnLogin  = document.getElementById('btn-login');
    const userInfo  = document.getElementById('user-info');
    const userNome  = document.getElementById('user-nome');

    state.usuario = usuario || null;
    if (usuario) {
      state.usuario = usuario;
      btnLogin.style.display  = 'none';
      userInfo.style.display  = 'flex';
      userNome.textContent    = usuario.nome || usuario.email;
      document.getElementById('tab-btn-admin').style.display = 'inline-block';
      document.getElementById('tab-btn-status').style.display = 'inline-block';
      document.getElementById('tab-btn-alertas').style.display = 'inline-block';
      await carregarCalendario(usuario);   // ← linha 635: inserir abrirWizard() ANTES desta linha
    } else {
      // ...
    }
  } catch (err) { /* ... */ }
}
```

De frontend/src/main.js linhas 814-866 — lógica btn-push-optin (COPIAR para ativarPushNotificacoes):
```javascript
// Lógica completa a extrair:
// 1. Checar suporte: !('Notification' in window) || !('serviceWorker' in navigator)
// 2. Checar Notification.permission === 'denied'
// 3. btn.disabled = true; btn.textContent = 'Solicitando permissão...';
// 4. await Notification.requestPermission()
// 5. if (!swRegistration) swRegistration = await navigator.serviceWorker.ready;
// 6. await api.push.getVapidPublicKey()
// 7. swRegistration.pushManager.subscribe({ userVisibleOnly: true, applicationServerKey })
// 8. await api.push.subscribe(subscription.toJSON())
// Diferença: recebe (feedbackEl, btnEl) como parâmetros em vez de buscar no DOM
```

De frontend/src/services/api.js — métodos disponíveis para o wizard:
```javascript
api.conectarCalendario()   // POST /api/calendar/connect — sem parâmetros
api.push.getVapidPublicKey()  // GET /api/push/vapid-public-key → { publicKey }
api.push.subscribe(subscriptionJSON)  // POST /api/push/subscribe
api.usuarios.setOnboardingDone()  // PATCH /api/usuarios/me { onboarding_done: 1 }
```

De frontend/src/styles/main.css — classes reutilizáveis para feedback:
```css
/* Reutilizar estas classes no .wizard-feedback */
.form-msg.success { background: #14532d; color: #86efac; }
.form-msg.error   { background: #7f1d1d; color: #fca5a5; }
```

CSS completo do wizard (UI-SPEC aprovada — usar EXATAMENTE este CSS, sem modificações):
```css
/* ── Phase 10: Onboarding Wizard ─────────────────────── */
#wizard-backdrop {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.65);
  z-index: 1000;
  display: flex;
  align-items: center;
  justify-content: center;
}

#wizard-modal {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 24px;
  max-width: 480px;
  width: calc(100vw - 32px);
  max-height: 90vh;
  overflow-y: auto;
  position: relative;
  z-index: 1001;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.6);
}

@keyframes wizard-shake {
  0%, 100% { transform: translateX(0); }
  25%       { transform: translateX(-6px); }
  75%       { transform: translateX(6px); }
}
#wizard-modal.shaking {
  animation: wizard-shake 0.2s ease-in-out 2;
}

.wizard-progress {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-bottom: 16px;
}
.wizard-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: var(--border);
  transition: background 0.2s, opacity 0.2s;
}
.wizard-dot.ativo    { background: var(--accent); }
.wizard-dot.visitado { background: var(--accent); opacity: 0.4; }

.wizard-progress-label {
  font-size: 0.75rem;
  color: var(--text-muted);
  margin-left: 4px;
}

#wizard-title {
  font-size: 1rem;
  font-weight: 600;
  color: var(--text);
  margin-bottom: 8px;
  line-height: 1.3;
}

.wizard-desc {
  font-size: 0.875rem;
  color: var(--text-muted);
  line-height: 1.5;
  margin-bottom: 16px;
}

.wizard-action-area {
  margin-bottom: 8px;
}

.wizard-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
  border-top: 1px solid var(--border);
  padding-top: 16px;
  margin-top: 24px;
}

#wizard-btn-pular {
  background: transparent;
  border: none;
  color: var(--text-muted);
  font-size: 0.875rem;
  cursor: pointer;
  padding: 12px 0;
  transition: color 0.15s;
}
#wizard-btn-pular:hover { color: var(--accent); }

#wizard-btn-proximo {
  background: var(--accent);
  color: #fff;
  border: none;
  border-radius: 6px;
  padding: 12px 24px;
  font-size: 0.875rem;
  font-weight: 600;
  cursor: pointer;
  min-width: 120px;
  transition: opacity 0.15s;
}
#wizard-btn-proximo:hover    { opacity: 0.85; }
#wizard-btn-proximo:disabled { opacity: 0.5; cursor: not-allowed; }

@media (max-width: 640px) {
  #wizard-modal { padding: 16px; }
  .wizard-footer { gap: 8px; }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Adicionar CSS do wizard em main.css</name>
  <files>frontend/src/styles/main.css</files>

  <read_first>
    - frontend/src/styles/main.css — para ver o final do arquivo e onde inserir o bloco do wizard (deve ser inserido no final do arquivo, após todos os estilos existentes)
  </read_first>

  <action>
Adicionar o bloco de CSS do wizard no FINAL de `frontend/src/styles/main.css`, após todos
os estilos existentes. Usar EXATAMENTE o CSS da seção `<interfaces>` acima (o CSS da UI-SPEC
aprovada) — não modificar nenhuma propriedade, valor, seletor ou comentário.

O bloco começa com:
```css
/* ── Phase 10: Onboarding Wizard ─────────────────────── */
#wizard-backdrop {
```

e termina com:
```css
@media (max-width: 640px) {
  #wizard-modal { padding: 16px; }
  .wizard-footer { gap: 8px; }
}
```

Não remover nem modificar nenhum estilo existente.
  </action>

  <verify>
    <automated>
      grep -n "wizard-backdrop" "frontend/src/styles/main.css"
    </automated>
  </verify>

  <acceptance_criteria>
    - frontend/src/styles/main.css contém `#wizard-backdrop`
    - frontend/src/styles/main.css contém `wizard-shake`
    - frontend/src/styles/main.css contém `.wizard-dot.ativo`
    - frontend/src/styles/main.css contém `#wizard-btn-pular`
    - frontend/src/styles/main.css contém `#wizard-btn-proximo`
    - frontend/src/styles/main.css contém `@media (max-width: 640px)` com regras do wizard
    - O bloco está no FINAL do arquivo (após estilos existentes)
  </acceptance_criteria>

  <done>CSS do wizard está em main.css; visual compila sem erros (npm run build ou vite).</done>
</task>

<task type="auto">
  <name>Tarefa 2: Implementar abrirWizard(), fecharWizard(), ativarPushNotificacoes() e disparo em carregarSessao()</name>
  <files>frontend/src/main.js</files>

  <read_first>
    - frontend/src/main.js — ler COMPLETO antes de modificar: precisará localizar:
      (a) linha 22 onde `swRegistration` é declarado (para confirmar escopo)
      (b) linhas 619-651 da função carregarSessao() para inserir a chamada ao wizard
      (c) linhas 814-866 do listener btn-push-optin para copiar a lógica para ativarPushNotificacoes()
      (d) um local adequado para declarar as três funções novas (antes de carregarSessao ou depois de carregarSessao, mas ANTES de qualquer chamada a init que invoque carregarSessao)
  </read_first>

  <action>
**Passo A — Extrair função ativarPushNotificacoes(feedbackEl, btnEl)**

Criar a função `ativarPushNotificacoes(feedbackEl, btnEl)` que encapsula a lógica do listener
`btn-push-optin` existente (linhas 814-866). O listener original NÃO deve ser modificado —
a nova função será chamada pelo wizard, e o listener existente continuará funcionando como está.

A função deve ter a seguinte assinatura e lógica:

```javascript
// Ativa push notifications. Usada pelo btn-push-optin e pelo wizard (passo 3).
// feedbackEl: elemento DOM onde exibir mensagens de sucesso/erro (role="status" ou "alert")
// btnEl: botão de ação do passo (para desabilitar durante loading e remover após sucesso)
async function ativarPushNotificacoes(feedbackEl, btnEl) {
  feedbackEl.textContent = '';
  feedbackEl.className = 'wizard-feedback';

  if (!('Notification' in window) || !('serviceWorker' in navigator)) {
    feedbackEl.className = 'wizard-feedback form-msg error';
    feedbackEl.setAttribute('role', 'alert');
    feedbackEl.textContent = 'Browser não suporta notificações push.';
    return;
  }
  if (Notification.permission === 'denied') {
    feedbackEl.className = 'wizard-feedback form-msg error';
    feedbackEl.setAttribute('role', 'alert');
    feedbackEl.textContent = 'Permissão de notificações bloqueada no browser. Acesse as configurações do navegador para liberar e tente novamente.';
    return;
  }

  btnEl.disabled = true;
  btnEl.textContent = 'Ativando...';

  try {
    const permission = await Notification.requestPermission();
    if (permission !== 'granted') {
      feedbackEl.className = 'wizard-feedback form-msg error';
      feedbackEl.setAttribute('role', 'alert');
      feedbackEl.textContent = 'Permissão de notificações bloqueada no browser. Acesse as configurações do navegador para liberar e tente novamente.';
      btnEl.disabled = false;
      btnEl.textContent = 'Ativar notificações';
      return;
    }

    if (!swRegistration) {
      swRegistration = await navigator.serviceWorker.ready;
    }

    const { publicKey: VAPID_PUBLIC_KEY } = await api.push.getVapidPublicKey();
    if (!VAPID_PUBLIC_KEY) throw new Error('VAPID_PUBLIC_KEY não configurada no servidor');

    const subscription = await swRegistration.pushManager.subscribe({
      userVisibleOnly: true,
      applicationServerKey: urlBase64ToUint8Array(VAPID_PUBLIC_KEY),
    });

    await api.push.subscribe(subscription.toJSON());
    feedbackEl.className = 'wizard-feedback form-msg success';
    feedbackEl.setAttribute('role', 'status');
    feedbackEl.textContent = 'Notificações ativadas.';
    btnEl.style.display = 'none'; // some após sucesso (conforme UI-SPEC)
    atualizarStatusPush('ativo', state.usuario?.alert_threshold || 51, state.usuario?.alert_hours_before ?? 24);
  } catch (err) {
    feedbackEl.className = 'wizard-feedback form-msg error';
    feedbackEl.setAttribute('role', 'alert');
    feedbackEl.textContent = 'Não foi possível ativar. Você pode tentar novamente na aba Calendário.';
    btnEl.disabled = false;
    btnEl.textContent = 'Ativar notificações';
  }
}
```

**Passo B — Criar fecharWizard()**

```javascript
function fecharWizard() {
  document.getElementById('wizard-backdrop')?.remove();
}
```

**Passo C — Criar abrirWizard()**

O modal é injetado por JS no DOM. O HTML do modal segue a estrutura da UI-SPEC. A função
gerencia os 3 passos, os listeners de interação e a lógica de cada passo.

```javascript
function abrirWizard() {
  // Não abrir se já estiver aberto
  if (document.getElementById('wizard-backdrop')) return;

  const PASSOS = [
    {
      titulo: 'Bem-vindo ao Floripa Alagamentos',
      desc: 'Este app monitora o risco de alagamento em tempo real nos bairros de Florianópolis e avisa você antes de ir a um lugar que pode estar alagado. Nos próximos 2 passos você conecta seu calendário e ativa os alertas.',
      btnProximo: 'Próximo',
      temAcao: false,
    },
    {
      titulo: 'Conecte seu Google Calendar',
      desc: 'O app verifica seus eventos das próximas 72h e calcula o risco de alagamento nos bairros onde eles acontecem. Seu calendário nunca é modificado.',
      btnProximo: 'Próximo',
      temAcao: true,
      btnAcaoTexto: 'Conectar Calendário',
    },
    {
      titulo: 'Ative as notificações push',
      desc: 'Receba alertas no seu dispositivo quando um evento do seu calendário cair em um bairro com risco elevado — mesmo com o app fechado.',
      btnProximo: 'Concluir',
      temAcao: true,
      btnAcaoTexto: 'Ativar notificações',
    },
  ];

  let passoAtual = 0; // índice 0-based

  function renderizarPasso() {
    const passo = PASSOS[passoAtual];
    const n = passoAtual + 1; // número 1-based para exibição

    // Atualizar dots
    const dots = document.querySelectorAll('.wizard-dot');
    dots.forEach((dot, i) => {
      dot.className = 'wizard-dot';
      if (i < passoAtual) dot.classList.add('visitado');
      else if (i === passoAtual) dot.classList.add('ativo');
    });

    // Atualizar label de progresso
    document.querySelector('.wizard-progress-label').textContent = `Passo ${n} de 3`;

    // Atualizar título e descrição
    document.getElementById('wizard-title').textContent = passo.titulo;
    document.querySelector('.wizard-desc').textContent = passo.desc;

    // Atualizar botão próximo/concluir
    document.getElementById('wizard-btn-proximo').textContent = passo.btnProximo;

    // Atualizar área de ação
    const actionArea = document.querySelector('.wizard-action-area');
    actionArea.innerHTML = '';
    const feedbackEl = document.querySelector('.wizard-feedback');
    feedbackEl.textContent = '';
    feedbackEl.className = 'wizard-feedback';

    if (passo.temAcao) {
      const btnAcao = document.createElement('button');
      btnAcao.className = 'btn-primary';
      btnAcao.style.cssText = 'margin-bottom: 12px;';
      btnAcao.textContent = passo.btnAcaoTexto;

      if (passoAtual === 1) {
        // Passo 2: Conectar Calendar
        btnAcao.addEventListener('click', async () => {
          btnAcao.disabled = true;
          btnAcao.textContent = 'Conectando...';
          try {
            await api.conectarCalendario();
            feedbackEl.className = 'wizard-feedback form-msg success';
            feedbackEl.setAttribute('role', 'status');
            feedbackEl.textContent = 'Calendário conectado com sucesso.';
            btnAcao.style.display = 'none'; // some após sucesso
            // NÃO chamar carregarSessao() aqui — fecharia o modal
            if (state.usuario) state.usuario.calendar_connected = 1;
          } catch (err) {
            feedbackEl.className = 'wizard-feedback form-msg error';
            feedbackEl.setAttribute('role', 'alert');
            if (err.status === 401 || (err.message && err.message.includes('permissão'))) {
              feedbackEl.textContent = 'Permissão de calendário não foi concedida no login. Saia e entre novamente para autorizar.';
            } else {
              feedbackEl.textContent = 'Não foi possível conectar. Você pode tentar novamente na aba Calendário.';
            }
            btnAcao.disabled = false;
            btnAcao.textContent = 'Conectar Calendário';
          }
        });
      } else if (passoAtual === 2) {
        // Passo 3: Ativar Push
        btnAcao.addEventListener('click', () => {
          ativarPushNotificacoes(feedbackEl, btnAcao);
        });
      }

      actionArea.appendChild(btnAcao);
    }

    // Atualizar focus trap para elementos corretos
    atualizarFocusTrap();
  }

  function atualizarFocusTrap() {
    // Reinstalar: o próprio keydown listener no modal cuida disso dinamicamente
  }

  // ── Construir HTML do backdrop + modal ────────────────
  const backdrop = document.createElement('div');
  backdrop.id = 'wizard-backdrop';
  backdrop.innerHTML = `
    <div id="wizard-modal" role="dialog" aria-modal="true" aria-labelledby="wizard-title" tabindex="-1">
      <div class="wizard-header">
        <div class="wizard-progress">
          <div class="wizard-dot ativo"></div>
          <div class="wizard-dot"></div>
          <div class="wizard-dot"></div>
          <span class="wizard-progress-label">Passo 1 de 3</span>
        </div>
        <h2 id="wizard-title"></h2>
      </div>
      <div class="wizard-body">
        <p class="wizard-desc"></p>
        <div class="wizard-action-area"></div>
        <div class="wizard-feedback" role="status"></div>
      </div>
      <div class="wizard-footer">
        <button id="wizard-btn-pular">Pular configuração</button>
        <button id="wizard-btn-proximo">Próximo</button>
      </div>
    </div>
  `;
  document.body.appendChild(backdrop);

  const modal = document.getElementById('wizard-modal');
  modal.focus();

  // ── Shake ao clicar fora ────────────────────────────
  backdrop.addEventListener('click', (e) => {
    if (e.target === backdrop) {
      modal.classList.add('shaking');
      modal.addEventListener('animationend', () => modal.classList.remove('shaking'), { once: true });
    }
  });

  // ── Escape não fecha ────────────────────────────────
  modal.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') {
      e.preventDefault();
      modal.classList.add('shaking');
      modal.addEventListener('animationend', () => modal.classList.remove('shaking'), { once: true });
      return;
    }
    // Focus trap
    if (e.key !== 'Tab') return;
    const focusable = [...modal.querySelectorAll(
      'button:not([disabled]):not([style*="display: none"]):not([style*="display:none"]), [tabindex]:not([tabindex="-1"])'
    )].filter(el => el.offsetParent !== null);
    if (focusable.length === 0) return;
    const first = focusable[0];
    const last = focusable[focusable.length - 1];
    if (e.shiftKey && document.activeElement === first) {
      e.preventDefault();
      last.focus();
    } else if (!e.shiftKey && document.activeElement === last) {
      e.preventDefault();
      first.focus();
    }
  });

  // ── Botão Pular ─────────────────────────────────────
  document.getElementById('wizard-btn-pular').addEventListener('click', async () => {
    fecharWizard();
    try {
      await api.usuarios.setOnboardingDone();
    } catch (_) {
      // Degradação silenciosa: wizard pode reaparecer no próximo login
    }
  });

  // ── Botão Próximo / Concluir ─────────────────────────
  document.getElementById('wizard-btn-proximo').addEventListener('click', async () => {
    if (passoAtual < PASSOS.length - 1) {
      passoAtual++;
      renderizarPasso();
    } else {
      // Passo 3: Concluir — gravar flag e fechar
      fecharWizard();
      try {
        await api.usuarios.setOnboardingDone();
      } catch (_) {
        // Degradação silenciosa
      }
    }
  });

  // Renderizar o primeiro passo
  renderizarPasso();
}
```

**Passo D — Inserir disparo do wizard em carregarSessao()**

Na função `carregarSessao()`, adicionar a chamada `abrirWizard()` dentro do bloco `if (usuario)`,
ANTES da linha `await carregarCalendario(usuario)`. O trecho modificado deve ficar assim:

```javascript
    if (usuario) {
      state.usuario = usuario;
      btnLogin.style.display  = 'none';
      userInfo.style.display  = 'flex';
      userNome.textContent    = usuario.nome || usuario.email;
      document.getElementById('tab-btn-admin').style.display = 'inline-block';
      document.getElementById('tab-btn-status').style.display = 'inline-block';
      document.getElementById('tab-btn-alertas').style.display = 'inline-block';
      // Onboarding wizard: abrir se usuário ainda não completou (UX-04, D-03)
      if (!usuario.onboarding_done) {
        abrirWizard();
      }
      await carregarCalendario(usuario);
    }
```

**Ordem de declaração das funções em main.js:**

As três funções novas (`fecharWizard`, `abrirWizard`, `ativarPushNotificacoes`) devem ser
declaradas ANTES da função `carregarSessao()` (que está na linha 619), em qualquer ponto
do arquivo antes dela. A posição recomendada é logo antes da seção `// ── Sessão / Auth ────`.
  </action>

  <verify>
    <automated>
      grep -n "abrirWizard\|fecharWizard\|ativarPushNotificacoes\|onboarding_done\|setOnboardingDone" "frontend/src/main.js"
    </automated>
  </verify>

  <acceptance_criteria>
    - frontend/src/main.js contém `function abrirWizard()`
    - frontend/src/main.js contém `function fecharWizard()`
    - frontend/src/main.js contém `async function ativarPushNotificacoes(feedbackEl, btnEl)`
    - frontend/src/main.js contém `if (!usuario.onboarding_done)` dentro de carregarSessao()
    - frontend/src/main.js contém `abrirWizard()` dentro de carregarSessao() (disparo)
    - frontend/src/main.js contém `await api.conectarCalendario()` dentro de abrirWizard()
    - frontend/src/main.js contém `ativarPushNotificacoes(feedbackEl, btnEl)` chamado no passo 3
    - frontend/src/main.js contém `await api.usuarios.setOnboardingDone()` em 2 lugares (pular e concluir)
    - frontend/src/main.js contém `e.key === 'Escape'` com prevenção de fechamento
    - frontend/src/main.js NÃO contém `carregarSessao()` dentro de abrirWizard() (pitfall 1)
    - a chamada `abrirWizard()` em carregarSessao() está ANTES de `await carregarCalendario(usuario)` (pitfall do RESEARCH.md)
  </acceptance_criteria>

  <done>
    Wizard modal abre automaticamente para usuário com onboarding_done === 0; todos os 3 passos
    funcionam com feedback inline; pular e concluir gravam o flag via PATCH; reload não reabre o wizard.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verificação humana: wizard funcional end-to-end</name>
  <what-built>
    Wizard modal de 3 passos completo: CSS (backdrop, modal, dots, botões, shake), lógica JS
    (abrirWizard, fecharWizard, ativarPushNotificacoes, disparo em carregarSessao), e backend
    (migration, PATCH endpoint, campo em /api/auth/me).
  </what-built>
  <how-to-verify>
    1. Iniciar o servidor: `cd backend && node src/app.js` (ou npm run dev)
    2. Iniciar o frontend: `cd frontend && npm run dev`
    3. Abrir o app em modo privado (garante sessão limpa)
    4. Fazer login com Google

    Verificar Passo 1:
    - O modal aparece automaticamente centralizado com backdrop escuro
    - Título: "Bem-vindo ao Floripa Alagamentos"
    - Dot 1 está em azul (ativo), dots 2 e 3 em cinza
    - Label mostra "Passo 1 de 3"
    - Botão "Pular configuração" visível à esquerda
    - Botão "Próximo" visível à direita em azul
    - Clicar fora do modal faz ele tremer mas não fecha
    - Pressionar Escape faz ele tremer mas não fecha

    Verificar Passo 2:
    - Clicar "Próximo" avança para o passo 2
    - Dot 1 fica com opacidade reduzida (visitado), dot 2 fica azul
    - Título: "Conecte seu Google Calendar"
    - Botão "Conectar Calendário" presente na área de ação
    - Clicar "Conectar Calendário": texto muda para "Conectando...", botão desabilita
    - Após sucesso: feedback verde "Calendário conectado com sucesso." aparece, botão some
    - Após erro: feedback vermelho com mensagem apropriada, botão volta ao normal
    - Botão "Próximo" permanece habilitado em todos os estados

    Verificar Passo 3:
    - Clicar "Próximo" avança para o passo 3
    - Dot 2 fica com opacidade reduzida, dot 3 fica azul, label "Passo 3 de 3"
    - Título: "Ative as notificações push"
    - Botão "Ativar notificações" presente
    - Botão primário agora diz "Concluir"
    - Clicar "Ativar notificações": solicita permissão do browser, exibe feedback

    Verificar Conclusão:
    - Clicar "Concluir" no passo 3: modal fecha
    - Recarregar a página: wizard NÃO reaparece
    - Abrir DevTools → Network: verificar que PATCH /api/usuarios/me foi chamado com { onboarding_done: 1 } e retornou { ok: true }

    Verificar Pular:
    - Em uma nova sessão (limpar cookies ou usar outro usuário de teste), fazer login
    - Clicar "Pular configuração" em qualquer passo: modal fecha imediatamente
    - Recarregar a página: wizard NÃO reaparece
  </how-to-verify>
  <resume-signal>Digite "aprovado" se o wizard funcionar corretamente, ou descreva os problemas encontrados para correção.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wizard frontend → PATCH /api/usuarios/me | Chamada feita pelo wizard ao pular ou concluir; protegida pelo requireAuth no backend (plano 10-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering | HTML do modal (injeção por JS) | accept | Copy é hardcoded em JS (não interpolado de user input); sem possibilidade de XSS via input do usuário |
| T-10-05 | Denial of Service | Wizard re-abre em loop se PATCH falhar | mitigate | fecharWizard() chamado antes do PATCH (degradação silenciosa aceitável); wizard só reaparece no próximo login se falhar, não em loop |
</threat_model>

<verification>
Verificações automatizadas após execução (antes do checkpoint humano):

1. `grep -c "wizard-backdrop" frontend/src/styles/main.css` — deve retornar pelo menos 1
2. `grep -c "wizard-shake" frontend/src/styles/main.css` — deve retornar pelo menos 1
3. `grep -c "abrirWizard" frontend/src/main.js` — deve retornar pelo menos 2 (declaração + chamada)
4. `grep -n "onboarding_done" frontend/src/main.js` — deve mostrar linha em carregarSessao()
5. `grep -n "setOnboardingDone" frontend/src/main.js` — deve mostrar 2 ocorrências (pular e concluir)
6. `grep -n "carregarSessao" frontend/src/main.js` — NÃO deve aparecer dentro de abrirWizard()
7. `cd frontend && npm run build` — build sem erros
</verification>

<success_criteria>
- Wizard abre automaticamente no primeiro login (onboarding_done = 0)
- 3 passos navegáveis com indicador de progresso (dots + texto "Passo X de 3")
- Passo 2: conectar calendar via api.conectarCalendario() com feedback inline
- Passo 3: ativar push reutilizando lógica de btn-push-optin com feedback inline
- Pular em qualquer passo: fecha modal + PATCH { onboarding_done: 1 }
- Concluir no passo 3: fecha modal + PATCH { onboarding_done: 1 }
- Reload após concluir ou pular: wizard não reaparece
- Clicar fora: shake animation, modal permanece
- Escape: shake animation, modal permanece
- Tab: focus trap circula apenas dentro do modal
- Build do frontend sem erros
</success_criteria>

<output>
Após conclusão e aprovação no checkpoint humano, criar `.planning/phases/10-onboarding-wizard/10-02-SUMMARY.md`
seguindo o template padrão em @$HOME/.claude/get-shit-done/templates/summary.md.
</output>
