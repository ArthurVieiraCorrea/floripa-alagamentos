---
phase: 11-landing-page
plan: 02
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - frontend/src/pages/landing.html
  - frontend/src/styles/landing.css
autonomous: true
requirements:
  - LP-01
  - LP-02
  - LP-03
  - LP-04
  - LP-05
  - LP-06
must_haves:
  truths:
    - "Visitante vê H1 com UVP em 1 frase sem clicar em nada"
    - "Visitante vê stat âncora R$1 bilhão/ano em cor amarela no hero"
    - "Visitante vê 4 cards na seção Como funciona com títulos e descrições dos 4 passos"
    - "Visitante vê 3 cards de horizonte na seção Para onde vamos sem datas absolutas"
    - "Visitante vê botão CTA Agendar uma demo que leva ao Calendly (placeholder) e email fallback"
    - "Visitante vê footer com copyright e link Acessar o app href=/app"
    - "Landing é legível e sem scroll horizontal em viewport 375px"
    - "Nav sticky com logo Floripa Alagamentos e link Entrar href=/app"
  artifacts:
    - path: "frontend/src/pages/landing.html"
      provides: "Landing page completa com 5 seções"
      contains: "Saiba antes de sair"
    - path: "frontend/src/styles/landing.css"
      provides: "Todos os estilos da landing incluindo responsividade"
      contains: "@media (max-width: 640px)"
  key_links:
    - from: "Nav link Entrar"
      to: "/app"
      via: "href=/app"
      pattern: "href=\"/app\""
    - from: "CTA Agendar uma demo"
      to: "https://calendly.com/SEU_LINK"
      via: "href no botão ou link"
      pattern: "calendly\\.com"
    - from: "Footer link"
      to: "/app"
      via: "href=/app"
      pattern: "Acessar o app"
    - from: "landing.html"
      to: "landing.css"
      via: "<link rel=stylesheet>"
      pattern: "landing\\.css"
---

<objective>
Completar landing.html com todas as 5 seções aprovadas no UI-SPEC e landing.css com todos os estilos e responsividade — transformando o stub do Plano 1 na landing page completa pronta para apresentação a investidores.

Purpose: LP-01 a LP-06 dependem do conteúdo e estilos finais. O conteúdo está completamente definido no UI-SPEC aprovado — nenhuma decisão de design está pendente.
Output: landing.html com Nav + Hero + Como funciona + Para onde vamos + CTA + Footer; landing.css com todos os estilos incluindo breakpoint 640px.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-landing-page/11-UI-SPEC.md
@.planning/phases/11-landing-page/11-RESEARCH.md
@.planning/phases/11-landing-page/11-01-SUMMARY.md

<interfaces>
<!-- Contratos de tokens CSS disponíveis via tokens.css (criado no Plano 1) -->

De frontend/src/styles/tokens.css (criado no Plano 1):
```css
:root {
  --bg: #0f172a;          /* fundo de página */
  --surface: #1e293b;     /* cards, nav, footer */
  --surface2: #263549;    /* placeholders, badge background */
  --border: #334155;      /* bordas de card e divisores */
  --text: #e2e8f0;        /* texto principal */
  --text-muted: #94a3b8;  /* texto secundário, captions */
  --accent: #3b82f6;      /* botão CTA, links ativos, step numbers */

  --baixo:   #22c55e;
  --medio:   #f59e0b;
  --alto:    #ef4444;
  --critico: #7c3aed;

  --risco-verde:     #22c55e;
  --risco-amarelo:   #f59e0b;   /* stat âncora R$1bi/ano */
  --risco-laranja:   #f97316;
  --risco-vermelho:  #ef4444;
  --risco-sem-dados: #cbd5e1;
}
```

Copywriting exato aprovado (11-UI-SPEC.md):
- H1: "Saiba antes de sair. Alertas de alagamento para Florianópolis."
- Stat âncora: "R$1 bilhão/ano em perdas por desastres em Santa Catarina"
- Sub-stat: "Florianópolis registrou dezenas de eventos de alagamento por bairro nos últimos 12 meses."
- CTA principal: "Agendar uma demo"
- Nav link app: "Entrar"
- H2 seção 2: "Como funciona"
- H2 seção 3: "Para onde vamos"
- H2 seção 4: "Quer ver ao vivo?"
- CTA subtítulo: "Agende uma demonstração de 20 minutos e veja o sistema funcionando com dados reais de Florianópolis."
- Email fallback: "Prefere email? Fale em arthur13082002@gmail.com"
- Footer: "© 2026 Floripa Alagamentos — Florianópolis, SC"
- Footer link: "Acessar o app →"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Implementar landing.css completo com todos os estilos e responsividade</name>
  <files>frontend/src/styles/landing.css</files>
  <read_first>
    - frontend/src/styles/landing.css (stub do Plano 1 — verificar que @import tokens.css está na linha 1)
    - .planning/phases/11-landing-page/11-UI-SPEC.md (Design System, Spacing Scale, Typography, Color, Layout sections — referência de todos os valores exatos)
  </read_first>
  <action>
Substituir o conteúdo stub de landing.css pelo CSS completo abaixo. O arquivo deve iniciar com `@import './tokens.css';` e conter todos os estilos das 5 seções.

```css
@import './tokens.css';

/* ── Reset e base ──────────────────────────────────────── */
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

html {
  font-family: 'Inter', system-ui, sans-serif;
  background: var(--bg);
  color: var(--text);
  scroll-behavior: smooth;
}

body { min-height: 100vh; }

/* ── Layout container ──────────────────────────────────── */
.container {
  max-width: 1024px;
  margin: 0 auto;
  padding: 0 16px;
}

/* ── Transições globais ────────────────────────────────── */
a, button {
  transition: opacity 0.15s, border-color 0.15s, color 0.15s;
}

/* ── Nav ───────────────────────────────────────────────── */
.nav {
  position: sticky;
  top: 0;
  background: var(--surface);
  border-bottom: 1px solid var(--border);
  padding: 8px 16px;
  z-index: 100;
}

.nav-inner {
  max-width: 1024px;
  margin: 0 auto;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.nav-logo {
  font-size: 1rem;
  font-weight: 600;
  color: var(--text);
  text-decoration: none;
}

.nav-link {
  font-size: 1rem;
  color: var(--text-muted);
  text-decoration: none;
}

.nav-link:hover { color: var(--accent); }

/* ── Hero ──────────────────────────────────────────────── */
.hero {
  padding: 64px 16px;
  text-align: center;
  background: var(--bg);
}

.hero-inner {
  max-width: 1024px;
  margin: 0 auto;
}

.hero h1 {
  font-size: clamp(1.75rem, 5vw, 2.25rem);
  font-weight: 600;
  line-height: 1.15;
  margin-bottom: 24px;
  color: var(--text);
}

.hero-stat {
  font-size: clamp(1.75rem, 5vw, 2.25rem);
  font-weight: 600;
  line-height: 1.15;
  color: var(--risco-amarelo);
  margin-bottom: 16px;
}

.hero-substat {
  font-size: 1rem;
  color: var(--text-muted);
  line-height: 1.6;
  margin-bottom: 32px;
}

/* ── Botão CTA ─────────────────────────────────────────── */
.btn-cta {
  display: inline-block;
  background: var(--accent);
  color: #fff;
  font-family: inherit;
  font-size: 1rem;
  font-weight: 600;
  padding: 16px 32px;
  border-radius: 8px;
  border: none;
  cursor: pointer;
  text-decoration: none;
  min-height: 44px;
  line-height: 1;
}

.btn-cta:hover { opacity: 0.85; }

.btn-cta:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
}

/* ── Seções genéricas ──────────────────────────────────── */
.section {
  padding: 64px 16px;
}

.section-alt {
  padding: 64px 16px;
  background: var(--surface);
}

.section-title {
  font-size: 1.25rem;
  font-weight: 600;
  line-height: 1.3;
  margin-bottom: 32px;
  color: var(--text);
}

/* ── Seção Como Funciona ───────────────────────────────── */
.steps-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  gap: 32px;
}

.step-card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 24px;
}

.step-card:hover {
  border-color: var(--accent);
}

.step-number {
  color: var(--accent);
  font-size: 0.875rem;
  font-weight: 600;
  margin-bottom: 8px;
}

.step-title {
  font-size: 1.25rem;
  font-weight: 600;
  line-height: 1.3;
  margin-bottom: 12px;
  color: var(--text);
}

.step-desc {
  font-size: 1rem;
  color: var(--text-muted);
  line-height: 1.6;
  margin-bottom: 16px;
}

/* ── Screenshot placeholder ─────────────────────────────── */
.screenshot-placeholder {
  background: var(--surface2);
  border-radius: 8px;
  border: 1px solid var(--border);
  width: 100%;
  aspect-ratio: 16/9;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--text-muted);
  font-size: 0.875rem;
}

/* ── Seção Para onde vamos (Roadmap) ───────────────────── */
.horizons {
  display: flex;
  gap: 24px;
  flex-wrap: wrap;
}

.horizon-card {
  flex: 1;
  min-width: 200px;
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 24px;
}

.horizon-badge {
  display: inline-block;
  border-radius: 99px;
  padding: 4px 8px;
  font-size: 0.875rem;
  font-weight: 600;
  margin-bottom: 12px;
  background: var(--surface2);
}

.horizon-badge--agora {
  border-left: 3px solid var(--accent);
  color: var(--accent);
}

.horizon-badge--proxima {
  border-left: 3px solid var(--risco-amarelo);
  color: var(--risco-amarelo);
}

.horizon-badge--longo {
  border-left: 3px solid var(--text-muted);
  color: var(--text-muted);
}

.horizon-title {
  font-size: 1.25rem;
  font-weight: 600;
  line-height: 1.3;
  margin-bottom: 8px;
  color: var(--text);
}

.horizon-desc {
  font-size: 1rem;
  color: var(--text-muted);
  line-height: 1.6;
}

/* ── Seção CTA Final ───────────────────────────────────── */
.cta-section {
  padding: 64px 16px;
  background: var(--surface);
  text-align: center;
}

.cta-section h2 {
  font-size: 1.25rem;
  font-weight: 600;
  line-height: 1.3;
  margin-bottom: 16px;
  color: var(--text);
}

.cta-subtitle {
  font-size: 1rem;
  color: var(--text-muted);
  line-height: 1.6;
  margin-bottom: 32px;
  max-width: 560px;
  margin-left: auto;
  margin-right: auto;
}

.cta-email-fallback {
  display: block;
  margin-top: 16px;
  font-size: 0.875rem;
  color: var(--text-muted);
}

.cta-email-fallback a {
  color: var(--accent);
  text-decoration: underline;
}

.cta-email-fallback a:hover { opacity: 0.8; }

/* ── Footer ────────────────────────────────────────────── */
.footer {
  background: var(--surface);
  border-top: 1px solid var(--border);
  padding: 24px 16px;
  text-align: center;
  font-size: 0.875rem;
  color: var(--text-muted);
}

.footer a {
  color: var(--accent);
  text-decoration: none;
  margin-left: 8px;
}

.footer a:hover { opacity: 0.8; }

/* ── Responsividade 375px ──────────────────────────────── */
@media (max-width: 640px) {
  .hero { padding: 48px 16px; }
  .section { padding: 48px 16px; }
  .section-alt { padding: 48px 16px; }
  .cta-section { padding: 48px 16px; }

  .btn-cta { padding: 16px 24px; }

  .steps-grid { grid-template-columns: 1fr; }

  .horizons { flex-direction: column; }

  img, .screenshot-placeholder { max-width: 100%; }
}
```
  </action>
  <verify>
    Verificar que @import está na linha 1:
    `head -1 /c/Users/arthu/floripa-alagamentos/frontend/src/styles/landing.css`
    Resultado esperado: `@import './tokens.css';`

    Verificar breakpoint responsivo:
    `grep -c "max-width: 640px" /c/Users/arthu/floripa-alagamentos/frontend/src/styles/landing.css`
    Resultado esperado: 1

    Verificar tokens de cor (zero hex hardcoded exceto #fff no btn-cta):
    `grep -c "#[0-9a-fA-F]\{6\}" /c/Users/arthu/floripa-alagamentos/frontend/src/styles/landing.css`
    Resultado esperado: 1 (apenas #fff no btn-cta — todos os outros valores usam var(--*))

    Verificar classes das 5 seções:
    `grep -c "\.nav\|\.hero\|\.steps-grid\|\.horizons\|\.cta-section\|\.footer" /c/Users/arthu/floripa-alagamentos/frontend/src/styles/landing.css`
    Resultado esperado: >= 6
  </verify>
  <acceptance_criteria>
    - `landing.css` primeira linha é `@import './tokens.css';`
    - `landing.css` contém `@media (max-width: 640px)` com overrides para seções e grids
    - Botão `.btn-cta` tem `min-height: 44px` (LP-06 touch target)
    - `.steps-grid` tem `grid-template-columns: repeat(auto-fit, minmax(220px, 1fr))`
    - `.horizons` tem `flex-direction: column` dentro do breakpoint 640px
    - `.hero-stat` tem `color: var(--risco-amarelo)` (LP-02)
    - `.screenshot-placeholder` tem `aspect-ratio: 16/9` e `background: var(--surface2)`
    - Apenas um valor hex hardcoded (`#fff`) — todos os outros usam tokens CSS
  </acceptance_criteria>
  <done>landing.css completo com todos os estilos das 5 seções, responsividade 640px e touch targets LP-06</done>
</task>

<task type="auto">
  <name>Tarefa 2: Implementar landing.html completo com todas as 5 seções</name>
  <files>frontend/src/pages/landing.html</files>
  <read_first>
    - frontend/src/pages/landing.html (stub do Plano 1 — verificar estrutura atual antes de sobrescrever)
    - .planning/phases/11-landing-page/11-UI-SPEC.md (Copywriting Contract + seções Layout — referência de todo o copy exato)
  </read_first>
  <action>
Substituir o conteúdo stub de landing.html pelo HTML completo abaixo. Usar os copies exatos do UI-SPEC — não parafraseá-los.

```html
<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Floripa Alagamentos — Alertas de alagamento para Florianópolis</title>
    <meta name="description" content="Saiba antes de sair. Alertas antecipados de alagamento por bairro em Florianópolis, com integração ao Google Calendar." />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="../styles/landing.css" />
  </head>
  <body>

    <!-- ── Nav ─────────────────────────────────────────── -->
    <nav class="nav">
      <div class="nav-inner">
        <a href="/" class="nav-logo">Floripa Alagamentos</a>
        <a href="/app" class="nav-link">Entrar</a>
      </div>
    </nav>

    <!-- ── Hero ────────────────────────────────────────── -->
    <section class="hero" id="hero">
      <div class="hero-inner">
        <h1>Saiba antes de sair. Alertas de alagamento para Florianópolis.</h1>
        <p class="hero-stat">R$1 bilhão/ano em perdas por desastres em Santa Catarina</p>
        <p class="hero-substat">Florianópolis registrou dezenas de eventos de alagamento por bairro nos últimos 12 meses.</p>
        <!-- TODO: substituir pela URL real do Calendly -->
        <a href="https://calendly.com/SEU_LINK" class="btn-cta" target="_blank" rel="noopener">Agendar uma demo</a>
      </div>
    </section>

    <!-- ── Como Funciona ───────────────────────────────── -->
    <section class="section" id="como-funciona">
      <div class="container">
        <h2 class="section-title">Como funciona</h2>
        <div class="steps-grid">

          <article class="step-card">
            <p class="step-number">01</p>
            <div class="screenshot-placeholder" aria-label="Screenshot do mapa de risco">
              <span>[screenshot do app]</span>
            </div>
            <h3 class="step-title">Mapa de risco em tempo real</h3>
            <p class="step-desc">Veja quais bairros de Florianópolis estão com risco elevado agora — atualizado a cada hora com dados reais de precipitação.</p>
          </article>

          <article class="step-card">
            <p class="step-number">02</p>
            <div class="screenshot-placeholder" aria-label="Screenshot da integração com Google Calendar">
              <span>[screenshot do app]</span>
            </div>
            <h3 class="step-title">Conecte seu Google Calendar</h3>
            <p class="step-desc">O app lê seus eventos das próximas 72h e identifica automaticamente o bairro de destino.</p>
          </article>

          <article class="step-card">
            <p class="step-number">03</p>
            <div class="screenshot-placeholder" aria-label="Screenshot das notificações push">
              <span>[screenshot do app]</span>
            </div>
            <h3 class="step-title">Receba alertas antes de sair</h3>
            <p class="step-desc">Notificações push chegam com antecedência configurável (1h a 48h) quando seu destino está em área de risco.</p>
          </article>

          <article class="step-card">
            <p class="step-number">04</p>
            <div class="screenshot-placeholder" aria-label="Screenshot do score de risco por bairro">
              <span>[screenshot do app]</span>
            </div>
            <h3 class="step-title">Score de risco por bairro</h3>
            <p class="step-desc">Cada bairro recebe um score de 0 a 100 cruzando histórico de ocorrências e previsão horária de chuva.</p>
          </article>

        </div>
      </div>
    </section>

    <!-- ── Para onde vamos (Roadmap) ───────────────────── -->
    <section class="section-alt" id="roadmap">
      <div class="container">
        <h2 class="section-title">Para onde vamos</h2>
        <div class="horizons">

          <div class="horizon-card">
            <span class="horizon-badge horizon-badge--agora">Agora</span>
            <h3 class="horizon-title">Florianópolis</h3>
            <p class="horizon-desc">Sistema operacional com alertas por push, integração com Google Calendar e motor de risco baseado em histórico local.</p>
          </div>

          <div class="horizon-card">
            <span class="horizon-badge horizon-badge--proxima">Próxima fase</span>
            <h3 class="horizon-title">Santa Catarina</h3>
            <p class="horizon-desc">Expansão para os municípios com maior histórico de desastres hídricos no estado, com dados integrados da Defesa Civil.</p>
          </div>

          <div class="horizon-card">
            <span class="horizon-badge horizon-badge--longo">Visão de longo prazo</span>
            <h3 class="horizon-title">Brasil</h3>
            <p class="horizon-desc">Plataforma nacional de alertas antecipados de alagamentos urbanos, aberta para municípios e integrada a sistemas oficiais de monitoramento.</p>
          </div>

        </div>
      </div>
    </section>

    <!-- ── CTA Final ────────────────────────────────────── -->
    <section class="cta-section" id="demo">
      <div class="container">
        <h2>Quer ver ao vivo?</h2>
        <p class="cta-subtitle">Agende uma demonstração de 20 minutos e veja o sistema funcionando com dados reais de Florianópolis.</p>
        <!-- TODO: substituir pela URL real do Calendly -->
        <a href="https://calendly.com/SEU_LINK" class="btn-cta" target="_blank" rel="noopener">Agendar uma demo</a>
        <span class="cta-email-fallback">
          Prefere email? Fale em <a href="mailto:arthur13082002@gmail.com">arthur13082002@gmail.com</a>
        </span>
      </div>
    </section>

    <!-- ── Footer ───────────────────────────────────────── -->
    <footer class="footer">
      <p>© 2026 Floripa Alagamentos — Florianópolis, SC<a href="/app">Acessar o app →</a></p>
    </footer>

  </body>
</html>
```

Notas de implementação:
- URL do Calendly usa placeholder `https://calendly.com/SEU_LINK` com comentário `<!-- TODO: ... -->` — decisão tomada no STATE.md (A3 do RESEARCH.md)
- Screenshots usam `<div class="screenshot-placeholder">` — troca por `<img>` quando imagens reais estiverem disponíveis (decisão tomada no RESEARCH.md)
- Sem `<script type="module">` — landing é 100% estática, zero JS
- Sem registro de service worker — conforme RESEARCH.md Pitfall 4
  </action>
  <verify>
    Verificar copy exato do H1:
    `grep -c "Saiba antes de sair" /c/Users/arthu/floripa-alagamentos/frontend/src/pages/landing.html`
    Resultado esperado: 1

    Verificar stat âncora:
    `grep -c "R\$1 bilh" /c/Users/arthu/floripa-alagamentos/frontend/src/pages/landing.html`
    Resultado esperado: 1

    Verificar 4 cards de passo:
    `grep -c "step-card" /c/Users/arthu/floripa-alagamentos/frontend/src/pages/landing.html`
    Resultado esperado: 4

    Verificar 3 horizontes:
    `grep -c "horizon-card" /c/Users/arthu/floripa-alagamentos/frontend/src/pages/landing.html`
    Resultado esperado: 3

    Verificar links /app:
    `grep -c 'href="/app"' /c/Users/arthu/floripa-alagamentos/frontend/src/pages/landing.html`
    Resultado esperado: 2 (nav Entrar + footer Acessar o app)

    Verificar email fallback:
    `grep -c "arthur13082002@gmail.com" /c/Users/arthu/floripa-alagamentos/frontend/src/pages/landing.html`
    Resultado esperado: 1

    Verificar sem script module (sem JS do app):
    `grep -c 'type="module"' /c/Users/arthu/floripa-alagamentos/frontend/src/pages/landing.html`
    Resultado esperado: 0

    Verificar footer copyright:
    `grep -c "2026 Floripa Alagamentos" /c/Users/arthu/floripa-alagamentos/frontend/src/pages/landing.html`
    Resultado esperado: 1

    Build final após ambas as tarefas:
    `cd /c/Users/arthu/floripa-alagamentos/frontend && npm run build 2>&1 | tail -5`
    Resultado esperado: sem erros, "built in Xs"
  </verify>
  <acceptance_criteria>
    - landing.html contém H1 "Saiba antes de sair. Alertas de alagamento para Florianópolis." (LP-01)
    - landing.html contém "R$1 bilhão/ano em perdas por desastres em Santa Catarina" (LP-02)
    - landing.html contém exatamente 4 elementos `.step-card` com os 4 títulos e descrições do UI-SPEC (LP-03)
    - landing.html contém exatamente 3 elementos `.horizon-card` (Florianópolis, Santa Catarina, Brasil) sem datas absolutas (LP-04)
    - landing.html contém link para `https://calendly.com/SEU_LINK` E email `arthur13082002@gmail.com` (LP-05)
    - landing.html contém 2 links `href="/app"` (nav Entrar + footer Acessar o app)
    - landing.html NÃO contém `type="module"` (sem JS do app)
    - landing.html contém `lang="pt-BR"` no html element
    - `npm run build` em frontend/ conclui sem erros após as duas tarefas
  </acceptance_criteria>
  <done>
    landing.html completo com Nav, Hero, Como funciona (4 cards), Para onde vamos (3 horizontes), CTA final e Footer.
    Todos os copies do UI-SPEC presentes.
    LP-01 a LP-06 satisfeitos.
    Build final validado.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitante→landing | HTML/CSS estático puro — sem autenticação, sem cookies, sem JS fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Information Disclosure | Email arthur13082002@gmail.com no HTML | accept | Email do fundador intencional como CTA de contato; dado público por decisão de produto (LP-05) |
| T-11-05 | Tampering | URL Calendly placeholder no HTML | accept | Placeholder documentado com TODO comment; sem risco — não linka a serviço real ainda |
| T-11-06 | Denial of Service | Google Fonts CDN | accept | Se CDN indisponível, `system-ui` é o fallback na font stack — landing permanece legível |
</threat_model>

<verification>
Verificação final completa após execução do plano — confirmar todos os success criteria do ROADMAP:

```bash
# Success Criterion 1: Hero com UVP e dado âncora visíveis sem clicar
grep "Saiba antes de sair" /c/Users/arthu/floripa-alagamentos/frontend/src/pages/landing.html
grep "R\$1 bilh" /c/Users/arthu/floripa-alagamentos/frontend/src/pages/landing.html

# Success Criterion 2: 4 cards "Como funciona" com steps completos
grep -c "step-card" /c/Users/arthu/floripa-alagamentos/frontend/src/pages/landing.html
# Esperado: 4

# Success Criterion 3: Roadmap 3 horizontes sem datas absolutas
grep -c "horizon-card" /c/Users/arthu/floripa-alagamentos/frontend/src/pages/landing.html
# Esperado: 3
grep -c "202[0-9]\|20[3-9][0-9]" /c/Users/arthu/floripa-alagamentos/frontend/src/pages/landing.html
# Esperado: 1 (apenas o copyright "2026" no footer — sem datas de roadmap)

# Success Criterion 4: CTA de contato funcionando
grep "calendly.com" /c/Users/arthu/floripa-alagamentos/frontend/src/pages/landing.html
grep "arthur13082002@gmail.com" /c/Users/arthu/floripa-alagamentos/frontend/src/pages/landing.html

# Success Criterion 5: App sem regressão + landing legível em 375px
grep -c 'href="/app"' /c/Users/arthu/floripa-alagamentos/frontend/src/pages/landing.html
# Esperado: 2
grep "max-width: 640px" /c/Users/arthu/floripa-alagamentos/frontend/src/styles/landing.css

# Build final
cd /c/Users/arthu/floripa-alagamentos/frontend && npm run build 2>&1 | tail -8
ls dist/index.html dist/app.html
```
</verification>

<success_criteria>
- landing.html completo com todas as 5 seções conforme UI-SPEC aprovado
- landing.css com todos os estilos incluindo responsividade @media (max-width: 640px)
- LP-01: H1 "Saiba antes de sair. Alertas de alagamento para Florianópolis." visível no hero
- LP-02: "R$1 bilhão/ano em perdas por desastres em Santa Catarina" visível no hero em --risco-amarelo
- LP-03: 4 cards de passo com screenshot placeholders e copies exatos do UI-SPEC
- LP-04: 3 cards de horizonte (Floripa/SC/Brasil) com badges sem datas absolutas
- LP-05: Botão "Agendar uma demo" linkando Calendly placeholder + email fallback arthur13082002@gmail.com
- LP-06: @media (max-width: 640px) com grid 1 coluna, flex-col nos horizontes, padding reduzido
- Build final sem erros produz dist/index.html (landing completa) e dist/app.html (app sem regressão)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/11-landing-page/11-02-SUMMARY.md` com:
- Confirmação de cada LP-01 a LP-06 satisfeito
- Resultado do build final (output completo do npm run build)
- Confirmação de que dist/app.html mantém regressão zero
- Screenshot ou inspeção do HTML gerado nas seções principais
- Qualquer desvio do plano e razão
</output>
